{
  if (isDebugTracingEnabled) {
    LOG.info("Unlocking " + cc.getBuffer() + " for the fetching thread"+ (isBacktracking ? "; backtracking" : ""));
  }
  cache.releaseBuffer(cc.getBuffer());
  cc.setBuffer(null);
}
