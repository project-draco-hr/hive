{
  if (file != null) {
    try {
      file.close();
    }
 catch (    IOException ex) {
      LOG.info("Failed to close file; ignoring: " + ex.getMessage());
    }
  }
  if (pool != null) {
    pool.clear();
  }
}
