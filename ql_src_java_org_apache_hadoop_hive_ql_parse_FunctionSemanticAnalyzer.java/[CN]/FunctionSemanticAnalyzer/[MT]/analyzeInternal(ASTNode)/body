{
  if (ast.getToken().getType() == HiveParser.TOK_CREATEFUNCTION)   analyzeCreateFunction(ast);
  if (ast.getToken().getType() == HiveParser.TOK_DROPFUNCTION)   analyzeDropFunction(ast);
  LOG.info("analyze done");
}
