{
  String cleanup=unitTests.cleanup();
  String setup=unitTests.setup();
  if (cleanup == null) {
    cleanup="";
  }
  if (setup == null) {
    setup="";
  }
  if ((cleanup.length() > 0) || (setup.length() > 0)) {
    String result=runHive("-e",cleanup + "\n" + setup);
    if (result.length() > 0) {
      System.err.println(name + " SETUP:  " + result);
    }
  }
}
