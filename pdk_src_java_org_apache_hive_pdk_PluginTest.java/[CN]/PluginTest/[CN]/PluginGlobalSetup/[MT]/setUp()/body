{
  String result=runHive("-i",new File(testScriptDir,"cleanup.sql").toString(),"-i","metadata/add-jar.sql","-i","metadata/class-registration.sql","-f",new File(testScriptDir,"setup.sql").toString());
  if (result.length() > 0) {
    System.err.println("GLOBAL SETUP:  " + result);
  }
}
