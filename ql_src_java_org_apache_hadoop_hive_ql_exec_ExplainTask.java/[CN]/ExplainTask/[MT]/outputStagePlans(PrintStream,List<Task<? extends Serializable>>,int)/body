{
  out.print(indentString(indent));
  out.println("STAGE PLANS:");
  for (  Task<? extends Serializable> rootTask : rootTasks) {
    outputPlan(rootTask,out,work.getExtended(),new HashSet<Task<? extends Serializable>>(),indent + 2);
  }
}
