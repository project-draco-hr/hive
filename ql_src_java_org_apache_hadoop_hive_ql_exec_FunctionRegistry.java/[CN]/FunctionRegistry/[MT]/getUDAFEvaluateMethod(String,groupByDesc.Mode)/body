{
  Class<? extends UDAF> udaf=getUDAF(name);
  if (udaf == null)   return null;
  return FunctionRegistry.getMethodInternal(udaf,(mode == groupByDesc.Mode.COMPLETE || mode == groupByDesc.Mode.FINAL) ? "evaluate" : "evaluatePartial",true,new ArrayList<Class<?>>());
}
