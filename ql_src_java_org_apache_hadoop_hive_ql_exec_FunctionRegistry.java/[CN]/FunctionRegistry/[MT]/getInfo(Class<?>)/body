{
  for (  Map.Entry<String,FunctionInfo> ent : mFunctions.entrySet()) {
    FunctionInfo val=ent.getValue();
    if (val.getUDFClass() == fClass || val.getUDAFClass() == fClass) {
      return val;
    }
  }
  return null;
}
