{
  boolean isKey=false;
  Set<ImmutableBitSet> keys=RelMetadataQuery.getUniqueKeys(rel);
  if (keys != null) {
    for (    ImmutableBitSet key : keys) {
      if (key.equals(c)) {
        isKey=true;
        break;
      }
    }
  }
  return isKey;
}
