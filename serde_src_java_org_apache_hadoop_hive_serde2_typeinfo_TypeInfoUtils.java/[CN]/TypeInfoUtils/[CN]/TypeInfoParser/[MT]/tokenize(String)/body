{
  ArrayList<Token> tokens=new ArrayList<Token>(0);
  int begin=0;
  int end=1;
  while (end <= typeInfoString.length()) {
    if (end == typeInfoString.length() || !Character.isLetterOrDigit(typeInfoString.charAt(end - 1)) || !Character.isLetterOrDigit(typeInfoString.charAt(end))) {
      Token t=new Token();
      t.position=begin;
      t.text=typeInfoString.substring(begin,end);
      t.isAlphaDigit=Character.isLetterOrDigit(typeInfoString.charAt(begin));
      tokens.add(t);
      begin=end;
    }
    end++;
  }
  return tokens;
}
