{
  if (len1 != len2) {
    return false;
  }
  for (int index1=start1, index2=start2; len1 > 0; len1--, index1++, index2++) {
    if ((arg1[index1] & 0xff) != (arg2[index2] & 0xff)) {
      return false;
    }
  }
  return true;
}
