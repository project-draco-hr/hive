{
  if (literalList == null || literalList.size() == 0 || !(literalList.get(0) instanceof Integer)) {
    return literalList;
  }
  List<Object> result=new ArrayList<Object>();
  for (  Object o : literalList) {
    result.add(Long.valueOf(o.toString()));
  }
  return result;
}
