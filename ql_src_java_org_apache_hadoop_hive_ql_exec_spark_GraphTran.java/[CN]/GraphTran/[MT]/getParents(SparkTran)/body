{
  if (!invertedTransGraph.containsKey(tran) || invertedTransGraph.get(tran) == null) {
    throw new Exception("Cannot get parent transformations for " + tran);
  }
  return new LinkedList<SparkTran>(invertedTransGraph.get(tran));
}
