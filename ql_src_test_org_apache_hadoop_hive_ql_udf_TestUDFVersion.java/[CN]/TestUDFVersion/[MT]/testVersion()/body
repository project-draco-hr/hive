{
  UDFVersion udf=new UDFVersion();
  Text result=udf.evaluate();
  String expected=String.format("%s r%s",HiveVersionInfo.getVersion(),HiveVersionInfo.getRevision());
  assertEquals(expected,result.toString());
}
