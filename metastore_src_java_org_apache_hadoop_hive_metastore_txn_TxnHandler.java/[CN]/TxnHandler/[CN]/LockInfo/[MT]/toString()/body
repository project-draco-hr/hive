{
  return "extLockId:" + Long.toString(extLockId) + " intLockId:"+ intLockId+ " txnId:"+ Long.toString(txnId)+ " db:"+ db+ " table:"+ table+ " partition:"+ partition+ " state:"+ (state == null ? "null" : state.toString())+ " type:"+ (type == null ? "null" : type.toString());
}
