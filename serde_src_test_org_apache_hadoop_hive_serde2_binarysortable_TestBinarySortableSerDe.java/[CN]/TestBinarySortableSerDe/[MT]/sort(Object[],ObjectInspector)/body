{
  for (int i=0; i < structs.length; i++)   for (int j=i + 1; j < structs.length; j++)   if (ObjectInspectorUtils.compare(structs[i],oi,structs[j],oi) > 0) {
    Object t=structs[i];
    structs[i]=structs[j];
    structs[j]=t;
  }
}
