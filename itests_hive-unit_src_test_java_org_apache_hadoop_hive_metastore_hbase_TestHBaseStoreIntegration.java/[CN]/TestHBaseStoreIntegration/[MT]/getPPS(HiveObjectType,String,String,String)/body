{
switch (objectType) {
case GLOBAL:
    return store.getUserPrivilegeSet(userName,null);
case DATABASE:
  return store.getDBPrivilegeSet(dbName,userName,null);
case TABLE:
return store.getTablePrivilegeSet(dbName,tableName,userName,null);
default :
throw new RuntimeException("huh?");
}
}
