{
  while (true) {
    int b;
    try {
      b=(int)in.readByte();
    }
 catch (    EOFException e) {
      return streamStatus.EOF;
    }
    if (b == terminator) {
      return streamStatus.TERMINATED;
    }
    if (b == separator) {
      return streamStatus.NORMAL;
    }
    out.write(b);
  }
}
