{
  MiniDFSCluster miniDFSCluster=new MiniDFSCluster(conf,numDataNodes,format,racks);
  miniDFSCluster.getFileSystem().getClient().setKeyProvider(miniDFSCluster.getNameNode().getNamesystem().getProvider());
  cluster=new MiniDFSShim(miniDFSCluster);
  return cluster;
}
