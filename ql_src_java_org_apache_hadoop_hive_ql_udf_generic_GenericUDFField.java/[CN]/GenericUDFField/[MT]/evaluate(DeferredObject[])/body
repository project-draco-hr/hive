{
  if (arguments[0].get() == null) {
    r.set(0);
    return r;
  }
  for (int i=1; i < arguments.length; i++) {
    if (arguments[0].get().equals(arguments[i].get())) {
      r.set(i);
      return r;
    }
  }
  r.set(0);
  return r;
}
