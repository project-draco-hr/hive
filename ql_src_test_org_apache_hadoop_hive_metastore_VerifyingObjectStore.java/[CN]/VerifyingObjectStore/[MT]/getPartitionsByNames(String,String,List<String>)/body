{
  List<Partition> sqlResults=getPartitionsByNamesInternal(dbName,tblName,partNames,true,false);
  List<Partition> ormResults=getPartitionsByNamesInternal(dbName,tblName,partNames,false,true);
  verifyParts(sqlResults,ormResults);
  return sqlResults;
}
