{
switch (fieldID) {
case NODE:
    return getNode();
case CHILDREN:
  return getChildren();
case ADJACENCYTYPE:
return getAdjacencyType();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
