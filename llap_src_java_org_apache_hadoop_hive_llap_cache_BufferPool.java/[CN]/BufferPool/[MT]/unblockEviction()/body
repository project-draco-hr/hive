{
  if (evictionIsWaiting <= 0)   return;
synchronized (evictionNotifyObj) {
    if (evictionIsWaiting <= 0)     return;
    if (DebugUtils.isTraceCachingEnabled()) {
      Llap.LOG.info("Notifying eviction that some block has been unlocked");
    }
    evictionNotifyObj.notifyAll();
  }
}
