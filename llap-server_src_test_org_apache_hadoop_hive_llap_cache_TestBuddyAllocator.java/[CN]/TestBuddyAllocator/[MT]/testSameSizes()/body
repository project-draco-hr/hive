{
  int min=3, max=8, maxAlloc=1 << max;
  BuddyAllocator a=new BuddyAllocator(isDirect,isMapped,1 << min,maxAlloc,maxAlloc,maxAlloc,tmpDir,new DummyMemoryManager(),LlapDaemonCacheMetrics.create("test","1"));
  for (int i=max; i >= min; --i) {
    allocSameSize(a,1 << (max - i),i);
  }
}
