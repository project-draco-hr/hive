{
  MapJoinOperator mapJoinOp=MapJoinProcessor.convertMapJoin(parseContext.getOpParseCtx(),joinOp,pGraphContext.getJoinContext().get(joinOp),joinContext.getBigTablePosition(),false,false);
  parseContext.getMapJoinContext().put(mapJoinOp,parseContext.getJoinContext().get(joinOp));
  parseContext.getJoinContext().remove(joinOp);
  convertMapJoinToBucketMapJoin(mapJoinOp,joinContext);
  return mapJoinOp;
}
