{
  List<Map<String,String>> partitionList=new ArrayList<Map<String,String>>(partitions.size());
  for (  Partition partition : partitions)   partitionList.add(getPartitionKeyValues(table,partition));
  return partitionList;
}
