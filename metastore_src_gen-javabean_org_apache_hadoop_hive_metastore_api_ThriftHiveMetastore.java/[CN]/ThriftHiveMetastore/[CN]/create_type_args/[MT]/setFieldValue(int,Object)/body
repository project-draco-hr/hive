{
switch (fieldID) {
case TYPE:
    if (value == null) {
      unsetType();
    }
 else {
      setType((Type)value);
    }
  break;
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
