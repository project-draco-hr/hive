{
switch (fieldID) {
case DBNAME:
    return getDbname();
case NAME:
  return getName();
case DELETEDATA:
return new Boolean(isDeleteData());
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
