{
switch (fieldID) {
case DB_NAME:
    return getDb_name();
case TBL_NAME:
  return getTbl_name();
case NEW_PART:
return getNew_part();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
