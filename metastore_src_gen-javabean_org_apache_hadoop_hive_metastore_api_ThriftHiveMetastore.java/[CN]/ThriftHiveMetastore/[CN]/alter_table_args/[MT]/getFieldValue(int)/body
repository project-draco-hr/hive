{
switch (fieldID) {
case DBNAME:
    return getDbname();
case TBL_NAME:
  return getTbl_name();
case NEW_TBL:
return getNew_tbl();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
