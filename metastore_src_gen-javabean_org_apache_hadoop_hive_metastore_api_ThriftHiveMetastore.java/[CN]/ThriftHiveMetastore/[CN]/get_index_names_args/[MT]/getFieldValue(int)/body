{
switch (fieldID) {
case DB_NAME:
    return getDb_name();
case TBL_NAME:
  return getTbl_name();
case MAX_INDEXES:
return new Short(getMax_indexes());
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
