{
  Assert.assertFalse("The location of the partition: " + partition.getName() + " was a "+ "subdirectory of the location of the table: "+ table.getTableName(),partition.getPartitionPath().toString().startsWith(table.getPath().toString()));
}
