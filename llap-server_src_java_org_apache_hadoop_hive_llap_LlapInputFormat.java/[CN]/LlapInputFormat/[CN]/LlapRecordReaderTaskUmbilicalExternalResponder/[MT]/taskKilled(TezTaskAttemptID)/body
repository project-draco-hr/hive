{
  try {
    sendOrQueueEvent(LlapRecordReader.ReaderEvent.errorEvent("Received task killed event for task ID " + taskAttemptId));
  }
 catch (  Exception err) {
    LOG.error("Error during heartbeat responder:",err);
  }
}
