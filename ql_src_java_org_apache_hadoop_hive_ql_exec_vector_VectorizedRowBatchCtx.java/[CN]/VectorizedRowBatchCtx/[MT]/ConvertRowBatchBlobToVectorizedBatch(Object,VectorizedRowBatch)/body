{
  if (deserializer instanceof VectorizedSerde) {
    batch=((VectorizedSerde)deserializer).deserializeVector(rowBlob,deserializer.getObjectInspector(),batch);
  }
 else {
    throw new SerDeException("Not able to deserialize row batch. Serde does not implement VectorizedSerde");
  }
}
