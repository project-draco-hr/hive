{
  if (this == other)   return true;
  if (!(other instanceof MapTypeInfo)) {
    return false;
  }
  MapTypeInfo o=(MapTypeInfo)other;
  return o.getCategory().equals(getCategory()) && o.getMapKeyTypeInfo().equals(getMapKeyTypeInfo()) && o.getMapValueTypeInfo().equals(getMapValueTypeInfo());
}
