{
  if (numEntriesSinceCheck < FLUSH_CHECK_THRESHOLD || batch.size == 0) {
    return false;
  }
  updateAvgVariableSize(batch);
  numEntriesSinceCheck=0;
  return numEntriesHashTable * (fixedHashEntrySize + avgVariableSize) > maxHashTblMemory;
}
