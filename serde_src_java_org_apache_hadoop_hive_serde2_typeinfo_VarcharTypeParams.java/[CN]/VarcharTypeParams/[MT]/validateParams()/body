{
  if (length < 1) {
    throw new SerDeException("VARCHAR length must be positive");
  }
  if (length > HiveVarchar.MAX_VARCHAR_LENGTH) {
    throw new SerDeException("Length " + length + " exceeds max varchar length of "+ HiveVarchar.MAX_VARCHAR_LENGTH);
  }
}
