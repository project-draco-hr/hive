{
  boolean useZeroCopy=(conf != null) && HiveConf.getBoolVar(conf,HiveConf.ConfVars.HIVE_ORC_ZEROCOPY);
  return new EncodedReaderImpl(fileSystem,path,fileId,useZeroCopy,types,codec,bufferSize,rowIndexStride,lowLevelCache,qfCounters,consumer);
}
