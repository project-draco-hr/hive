{
  return metadataReaderFactory.create(MetadataReaderProperties.builder().withBufferSize(bufferSize).withCodec(codec).withFileSystem(fileSystem).withPath(path).withTypeCount(types.size()).build());
}
