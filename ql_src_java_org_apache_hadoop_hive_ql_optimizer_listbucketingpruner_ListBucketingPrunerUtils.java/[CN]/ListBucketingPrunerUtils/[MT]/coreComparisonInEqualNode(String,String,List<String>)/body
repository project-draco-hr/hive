{
  Boolean result;
  if (cellValueInPosition.equals(constantValueInFilter) && !cellValueInPosition.equals(ListBucketingPruner.DEFAULT_SKEWED_KEY)) {
    result=Boolean.TRUE;
  }
 else   if (cellValueInPosition.equals(ListBucketingPruner.DEFAULT_SKEWED_KEY) && !uniqSkewedValuesInPosition.contains(constantValueInFilter)) {
    result=null;
  }
 else {
    result=Boolean.FALSE;
  }
  return result;
}
