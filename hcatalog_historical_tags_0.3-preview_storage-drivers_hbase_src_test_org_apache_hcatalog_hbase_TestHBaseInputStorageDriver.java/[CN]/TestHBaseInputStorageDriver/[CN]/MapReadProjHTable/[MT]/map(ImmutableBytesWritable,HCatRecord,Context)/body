{
  boolean correctValues=(value.size() == 2) && (value.get(0).toString()).startsWith("testRow") && (value.get(1).toString()).startsWith("testQualifier1");
  if (correctValues == false) {
    error=true;
  }
}
