{
  try {
    if (!jobIsSetup) {
      this.outputCommitter.setupJob(taContext);
      jobIsSetup=true;
    }
    writer.write(arg0,arg1);
    this.wroteData=true;
  }
 catch (  Exception e) {
    throw new IOException(e);
  }
}
