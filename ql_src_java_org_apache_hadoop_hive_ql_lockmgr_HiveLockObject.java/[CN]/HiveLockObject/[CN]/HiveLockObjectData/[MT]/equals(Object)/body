{
  if (!(o instanceof HiveLockObjectData)) {
    return false;
  }
  HiveLockObjectData target=(HiveLockObjectData)o;
  boolean ret=(queryId == null) ? target.getQueryId() == null : queryId.equals(target.getQueryId());
  ret=ret && (lockTime == null) ? target.getLockTime() == null : queryId.equals(target.getLockTime());
  ret=ret && (lockMode == null) ? target.getLockMode() == null : queryId.equals(target.getLockMode());
  ret=ret && (queryStr == null) ? target.getQueryStr() == null : queryStr.equals(target.getQueryStr());
  ret=ret && (clientIp == null) ? target.getClientIp() == null : clientIp.equals(target.getClientIp());
  return ret;
}
