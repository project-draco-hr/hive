{
  if (!(o instanceof HiveLockObject)) {
    return false;
  }
  HiveLockObject tgt=(HiveLockObject)o;
  return getName().equals(tgt.getName()) && data == null ? tgt.getData() == null : data.equals(tgt.getData());
}
