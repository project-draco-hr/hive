{
  long newBlank=gen.nextLong();
  while (newBlank == blank || lookup(newBlank)) {
    newBlank=gen.nextLong();
  }
  for (int i=0; i != n; i++) {
    if (t1[i] == blank) {
      t1[i]=newBlank;
    }
    if (t2[i] == blank) {
      t2[i]=newBlank;
    }
  }
  blank=newBlank;
}
