{
  if (deserializer == null) {
    try {
      deserializer=MetaStoreUtils.getDeserializer(Hive.get().getConf(),getTPartition(),getTable().getTTable());
    }
 catch (    MetaException e) {
      throw new HiveException(e);
    }
  }
}
