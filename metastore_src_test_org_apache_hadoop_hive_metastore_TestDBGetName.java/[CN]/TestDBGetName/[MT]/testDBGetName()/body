{
  try {
    String dbname="default";
    DB db=new DB(dbname,conf_);
    assertTrue(db.getName().equals(dbname));
    cleanup();
  }
 catch (  MetaException e) {
    e.printStackTrace();
    throw e;
  }
}
