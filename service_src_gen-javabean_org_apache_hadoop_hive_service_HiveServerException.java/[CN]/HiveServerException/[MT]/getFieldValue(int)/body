{
switch (fieldID) {
case MESSAGE:
    return getMessage();
case ERRORCODE:
  return new Integer(getErrorCode());
case SQLSTATE:
return getSQLState();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
