{
  int hashCode=1;
  if (obj1 == null) {
    hashCode=metadataTag;
  }
 else {
    hashCode+=(31 + obj1.hashCode());
  }
  if (obj2 == null) {
    hashCode+=metadataTag;
  }
 else {
    hashCode+=(31 + obj2.hashCode());
  }
  return hashCode;
}
