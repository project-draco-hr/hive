{
  Collection<String> userFilter=conf.getStringCollection(HiveConf.ConfVars.HIVE_SERVER2_PLAIN_LDAP_USERFILTER.varname);
  if (userFilter.isEmpty()) {
    return null;
  }
  return new UserFilter(userFilter);
}
