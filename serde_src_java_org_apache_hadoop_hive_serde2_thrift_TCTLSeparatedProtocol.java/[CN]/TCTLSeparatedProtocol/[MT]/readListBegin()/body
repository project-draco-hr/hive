{
  assert(!inner);
  TList list=new TList();
  fields=secondaryPattern.split(columns[index++]);
  if (fields != null) {
    list.size=fields.length;
  }
 else {
    list.size=0;
  }
  innerIndex=0;
  inner=true;
  return list;
}
