{
  assert(!inner);
  TSet set=new TSet();
  fields=secondaryPattern.split(columns[index++]);
  if (fields != null) {
    set.size=fields.length;
  }
 else {
    set.size=0;
  }
  inner=true;
  innerIndex=0;
  return set;
}
