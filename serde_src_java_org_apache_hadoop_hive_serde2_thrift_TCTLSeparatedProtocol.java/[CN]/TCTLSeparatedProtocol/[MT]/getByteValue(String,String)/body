{
  if (altValue != null && altValue.length() > 0) {
    try {
      byte b[]=new byte[1];
      b[0]=Byte.valueOf(altValue).byteValue();
      return new String(b);
    }
 catch (    NumberFormatException e) {
      return altValue;
    }
  }
  return defaultVal;
}
