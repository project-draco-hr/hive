{
  assert(!inner);
  try {
    final String tmp=transportTokenizer.nextToken();
    columns=primaryPattern.split(tmp);
    index=0;
    return new TStruct();
  }
 catch (  EOFException e) {
    return null;
  }
}
