{
  assert(!inner);
  TMap map=new TMap();
  fields=mapPattern.split(columns[index++]);
  if (fields != null) {
    map.size=fields.length / 2;
  }
 else {
    map.size=0;
  }
  innerIndex=0;
  inner=true;
  isMap=true;
  return map;
}
