{
  if (deque.offer(e)) {
    return null;
  }
 else {
    E last=deque.peekLast();
    if (comparator.compare(e,last) < 0) {
      deque.removeLast();
      deque.offer(e);
      return last;
    }
    return e;
  }
}
