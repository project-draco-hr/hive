{
  List<RelDataType> fTypes=Lists.transform(structType.getAllStructFieldTypeInfos(),new Function<TypeInfo,RelDataType>(){
    @Override public RelDataType apply(    TypeInfo tI){
      return convert(tI,dtFactory);
    }
  }
);
  return dtFactory.createStructType(fTypes,structType.getAllStructFieldNames());
}
