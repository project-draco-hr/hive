{
  try {
    HiveIntervalDayTime interval=HiveIntervalDayTime.valueOf(new String(inV.vector[i],inV.start[i],inV.length[i],"UTF-8"));
    outV.setEpochSecondsAndSignedNanos(i,interval.getTotalSeconds(),interval.getNanos());
  }
 catch (  Exception e) {
    outV.setNullValue(i);
    outV.isNull[i]=true;
    outV.noNulls=false;
  }
}
