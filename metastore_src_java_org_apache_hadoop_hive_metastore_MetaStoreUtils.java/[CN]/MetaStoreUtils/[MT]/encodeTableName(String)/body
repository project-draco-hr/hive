{
  String ret="";
  for (  char ch : name.toCharArray()) {
    if (Character.isLetterOrDigit(ch) || ch == '_') {
      ret+=ch;
    }
 else {
      ret+="-" + (int)ch + "-";
    }
  }
  return ret;
}
