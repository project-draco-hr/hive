{
  if (!executionContext.getBadHosts().isEmpty()) {
    LOG.warn("Found bad nodes on FixedExecutionContext. Cannot replace them. Degraded performance. badNodes={}",executionContext.getBadHosts());
  }
}
