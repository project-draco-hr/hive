{
  URL url=null;
  try {
    if (StringUtils.indexOf(path,"file:/") == 0) {
      url=new URL(path);
    }
 else {
      url=new File(path).toURL();
    }
  }
 catch (  Exception err) {
    LOG.error("Bad URL " + path + ", ignoring path");
  }
  return url;
}
