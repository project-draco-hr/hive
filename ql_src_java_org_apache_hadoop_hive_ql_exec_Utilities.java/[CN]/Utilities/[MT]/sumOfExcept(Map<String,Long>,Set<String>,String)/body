{
  long total=0;
  for (  String alias : aliases) {
    if (alias.equals(except)) {
      continue;
    }
    Long size=aliasToSize.get(alias);
    if (size == null) {
      return -1;
    }
    total+=size;
  }
  return total;
}
