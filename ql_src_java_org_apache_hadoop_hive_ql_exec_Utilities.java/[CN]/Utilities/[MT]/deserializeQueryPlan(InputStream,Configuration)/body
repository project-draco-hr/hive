{
  XMLDecoder d=null;
  try {
    d=new XMLDecoder(in,null,null,addResourceFilesToClassPath(conf));
    QueryPlan ret=(QueryPlan)d.readObject();
    return (ret);
  }
  finally {
    if (null != d) {
      d.close();
    }
  }
}
