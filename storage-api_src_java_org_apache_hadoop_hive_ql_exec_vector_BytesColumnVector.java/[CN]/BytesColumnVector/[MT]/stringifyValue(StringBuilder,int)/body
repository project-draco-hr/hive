{
  if (isRepeating) {
    row=0;
  }
  if (noNulls || !isNull[row]) {
    buffer.append('"');
    buffer.append(new String(this.buffer,start[row],length[row]));
    buffer.append('"');
  }
 else {
    buffer.append("null");
  }
}
