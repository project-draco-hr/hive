{
  if (path == null) {
    return null;
  }
  URL url=null;
  try {
    URI uri=new URI(path);
    if (uri.getScheme() != null) {
      url=uri.toURL();
    }
 else {
      url=new File(path).toURI().toURL();
    }
  }
 catch (  URISyntaxException e) {
  }
  return url;
}
