{
  if (isList) {
    return (unitField.getName() + "[" + (isSlice ? lowIndex + ":" + highIndex : lowIndex)+ "]");
  }
 else   if (isMap) {
    return (unitField.getName() + ((keyString != null) ? "[" + keyString + "]" : ""));
  }
 else {
    return unitField.getName();
  }
}
