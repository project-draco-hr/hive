{
  if (data == null) {
    return -1;
  }
  boolean isArray=!(data instanceof List);
  if (isArray) {
    Object[] list=(Object[])data;
    return list.length;
  }
 else {
    List<?> list=(List<?>)data;
    return list.size();
  }
}
