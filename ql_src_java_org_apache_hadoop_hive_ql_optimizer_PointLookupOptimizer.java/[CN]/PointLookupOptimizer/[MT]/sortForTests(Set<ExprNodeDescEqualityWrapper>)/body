{
  if (!testMode) {
    return valuesExpr;
  }
  final Collection<ExprNodeDescEqualityWrapper> sortedValues;
  sortedValues=ImmutableSortedSet.copyOf(new Comparator<ExprNodeDescEqualityWrapper>(){
    @Override public int compare(    ExprNodeDescEqualityWrapper w1,    ExprNodeDescEqualityWrapper w2){
      if (w1.equals(w2)) {
        return 0;
      }
      return w1.getExprNodeDesc().getExprString().compareTo(w2.getExprNodeDesc().getExprString());
    }
  }
,valuesExpr);
  return sortedValues;
}
