{
  runAs=user;
  List<String> args=makeArgs(execute,srcFile,defines,hiveArgs,otherFiles,statusdir,completedUrl,enablelog,enableJobReconnect);
  return enqueueController(user,userArgs,callback,args);
}
