{
  byte result=bytes[offset++];
  fieldIndex++;
  if (fieldIndex < fieldCount) {
    if (offset >= end) {
      warnBeyondEof();
    }
    if ((fieldIndex % 8) == 0) {
      nullByte=bytes[offset++];
    }
  }
  return result;
}
