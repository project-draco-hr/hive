{
  PrunedPartitionList partsList=opToPartList.get(ts);
  if (partsList == null) {
    partsList=PartitionPruner.prune(topToTable.get(ts),opToPartPruner.get(ts),conf,alias,prunedPartitions);
    opToPartList.put(ts,partsList);
  }
  return partsList;
}
