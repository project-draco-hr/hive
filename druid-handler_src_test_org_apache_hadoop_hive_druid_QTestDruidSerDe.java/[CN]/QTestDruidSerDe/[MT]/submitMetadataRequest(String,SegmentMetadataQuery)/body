{
  List<SegmentAnalysis> resultsList;
  try {
    resultsList=DruidStorageHandlerUtils.JSON_MAPPER.readValue(RESPONSE,new TypeReference<List<SegmentAnalysis>>(){
    }
);
  }
 catch (  Exception e) {
    throw new SerDeException(StringUtils.stringifyException(e));
  }
  return resultsList.get(0);
}
