{
  if (nonFinalPreds == null || nonFinalPreds.isEmpty()) {
    return false;
  }
  for (  List<ExprNodeDesc> exprs : nonFinalPreds.values()) {
    if (!exprs.isEmpty()) {
      return true;
    }
  }
  return false;
}
