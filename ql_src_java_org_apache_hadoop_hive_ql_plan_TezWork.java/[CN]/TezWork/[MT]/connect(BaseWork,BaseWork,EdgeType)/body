{
  workGraph.get(a).add(b);
  invertedWorkGraph.get(b).add(a);
  roots.remove(b);
  leaves.remove(a);
switch (edgeType) {
case BROADCAST_EDGE:
    broadcastEdge.get(a).add(b);
  break;
default :
break;
}
}
