{
  this.loader=loader;
  int threadCount=HiveConf.getIntVar(conf,HiveConf.ConfVars.LLAP_REQUEST_THREAD_COUNT);
  this.threadPool=Executors.newFixedThreadPool(threadCount,new ThreadFactoryBuilder().setDaemon(true).setNameFormat("Llap thread %d").build());
}
