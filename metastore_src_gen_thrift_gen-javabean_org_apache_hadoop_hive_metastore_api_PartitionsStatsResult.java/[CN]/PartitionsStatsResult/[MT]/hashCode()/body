{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_partStats=true && (isSetPartStats());
  builder.append(present_partStats);
  if (present_partStats)   builder.append(partStats);
  return builder.toHashCode();
}
