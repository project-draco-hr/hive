{
  int numElems=stack.size();
  String name=new String();
  for (int pos=numElems - 1; pos >= 0; pos--) {
    name=stack.get(pos).getOperatorName().concat(name);
    Matcher m=pattern.matcher(name);
    if (m.matches())     return m.group().length();
  }
  return -1;
}
