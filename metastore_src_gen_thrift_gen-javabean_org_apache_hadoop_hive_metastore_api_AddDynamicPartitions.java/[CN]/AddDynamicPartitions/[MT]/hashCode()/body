{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_txnid=true;
  builder.append(present_txnid);
  if (present_txnid)   builder.append(txnid);
  boolean present_dbname=true && (isSetDbname());
  builder.append(present_dbname);
  if (present_dbname)   builder.append(dbname);
  boolean present_tablename=true && (isSetTablename());
  builder.append(present_tablename);
  if (present_tablename)   builder.append(tablename);
  boolean present_partitionnames=true && (isSetPartitionnames());
  builder.append(present_partitionnames);
  if (present_partitionnames)   builder.append(partitionnames);
  return builder.toHashCode();
}
