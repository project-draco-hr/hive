{
  final int N=100000;
  final long MASK56=0x00FFFFFFFFFFFFL;
  int seed=897089790;
  Random rand=new Random(seed);
  long l1, l2;
  for (int i=1; i <= N; i++) {
    l1=rand.nextLong() & MASK56;
    l2=rand.nextLong() & MASK56;
    verifyMultiplyDivideInverse(l1,l2);
  }
}
