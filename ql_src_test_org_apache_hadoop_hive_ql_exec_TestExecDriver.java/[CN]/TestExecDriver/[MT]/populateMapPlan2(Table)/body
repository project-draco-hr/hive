{
  mr.setNumReduceTasks(Integer.valueOf(0));
  Operator<fileSinkDesc> op3=OperatorFactory.get(new fileSinkDesc(tmpdir + "mapplan2.out",Utilities.defaultTd));
  Operator<scriptDesc> op2=OperatorFactory.get(new scriptDesc("/bin/cat",new tableDesc(MetadataTypedColumnsetSerDe.class,null,null,Utilities.makeProperties("serialization.format","9","columns","key,value"))),op3);
  Operator<filterDesc> op1=OperatorFactory.get(getTestFilterDesc("key"),op2);
  Utilities.addMapWork(mr,src,"a",op1);
}
