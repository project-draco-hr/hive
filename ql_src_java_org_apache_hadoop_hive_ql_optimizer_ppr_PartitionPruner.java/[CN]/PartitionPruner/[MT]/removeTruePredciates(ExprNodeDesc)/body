{
  if (e instanceof ExprNodeConstantDesc) {
    ExprNodeConstantDesc eC=(ExprNodeConstantDesc)e;
    if (e.getTypeInfo() == TypeInfoFactory.booleanTypeInfo && eC.getValue() == Boolean.TRUE) {
      return null;
    }
  }
  return e;
}
