{
  super.serviceInit(conf);
  int numThreads=conf.getInt(LlapDaemonConfiguration.LLAP_DAEMON_COMMUNICATOR_NUM_THREADS,LlapDaemonConfiguration.LLAP_DAEMON_COMMUNICATOR_NUM_THREADS_DEFAULT);
  this.communicator=new TaskCommunicator(numThreads);
  this.communicator.init(conf);
}
