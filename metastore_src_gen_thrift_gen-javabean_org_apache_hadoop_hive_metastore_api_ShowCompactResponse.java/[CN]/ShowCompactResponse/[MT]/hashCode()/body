{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_compacts=true && (isSetCompacts());
  builder.append(present_compacts);
  if (present_compacts)   builder.append(compacts);
  return builder.toHashCode();
}
