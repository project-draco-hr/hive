{
  if (isLogInfoEnabled) {
    cntr+=counter;
    if (cntr >= nextCntr) {
      LOG.info(id + " forwarding " + cntr+ " rows");
      do {
        nextCntr=getNextCntr(nextCntr);
      }
 while (cntr >= nextCntr);
    }
  }
}
