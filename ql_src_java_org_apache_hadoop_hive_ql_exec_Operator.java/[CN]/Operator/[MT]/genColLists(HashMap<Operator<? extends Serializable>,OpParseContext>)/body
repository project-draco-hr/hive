{
  List<String> colList=new ArrayList<String>();
  if (childOperators != null)   for (  Operator<? extends Serializable> o : childOperators)   colList=Utilities.mergeUniqElems(colList,o.genColLists(opParseCtx));
  List<String> cols=mergeColListsFromChildren(colList,opParseCtx);
  OpParseContext ctx=opParseCtx.get(this);
  ctx.setColNames(cols);
  return cols;
}
