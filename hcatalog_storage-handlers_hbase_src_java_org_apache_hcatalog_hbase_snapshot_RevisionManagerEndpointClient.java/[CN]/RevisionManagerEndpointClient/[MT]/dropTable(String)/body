{
  call(new Batch.Call<RevisionManagerEndpointService,Void>(){
    @Override public Void call(    RevisionManagerEndpointService service) throws IOException {
      ServerRpcController controller=new ServerRpcController();
      BlockingRpcCallback<DropTableResponse> done=new BlockingRpcCallback<DropTableResponse>();
      DropTableRequest request=DropTableRequest.newBuilder().setTableName(table).build();
      service.dropTable(null,request,done);
      blockOnResponse(done,controller);
      return null;
    }
  }
);
}
