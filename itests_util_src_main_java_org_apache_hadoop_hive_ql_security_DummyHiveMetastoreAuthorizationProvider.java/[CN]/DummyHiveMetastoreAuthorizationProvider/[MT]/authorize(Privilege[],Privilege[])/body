{
  debugLog("DHMAP.authorize " + "read:" + debugPrivPrint(readRequiredPriv) + " , write:"+ debugPrivPrint(writeRequiredPriv));
  authCalls.add(new AuthCallContext(AuthCallContextType.USER,readRequiredPriv,writeRequiredPriv));
}
