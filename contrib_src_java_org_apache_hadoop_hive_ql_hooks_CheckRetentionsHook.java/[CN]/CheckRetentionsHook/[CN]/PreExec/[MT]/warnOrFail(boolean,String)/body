{
  if (warning) {
    System.err.println("\n ----------");
    System.err.println("| WARNING: | ");
    System.err.println(" ----------");
    System.err.println("  This command does NOT comply with the RETENTION " + "policies. This command will fail in the near future. \n" + mesg);
  }
 else {
    throw new Exception(mesg);
  }
}
