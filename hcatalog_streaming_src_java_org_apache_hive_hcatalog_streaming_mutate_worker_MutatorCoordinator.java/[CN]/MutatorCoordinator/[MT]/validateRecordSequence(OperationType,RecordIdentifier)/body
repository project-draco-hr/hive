{
  boolean identiferOutOfSequence=operationType != OperationType.INSERT && !sequenceValidator.isInSequence(newRecordIdentifier);
  if (identiferOutOfSequence) {
    throw new RecordSequenceException("Records not in sequence: state=" + sequenceValidator + ", recordIdentifier="+ newRecordIdentifier);
  }
}
