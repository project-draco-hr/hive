{
  if (!groupingValidator.isInSequence(newPartitionValues,bucketId)) {
    throw new GroupRevisitedException("Group out of sequence: state=" + groupingValidator + ", partition="+ newPartitionValues+ ", bucketId="+ newBucketId);
  }
}
