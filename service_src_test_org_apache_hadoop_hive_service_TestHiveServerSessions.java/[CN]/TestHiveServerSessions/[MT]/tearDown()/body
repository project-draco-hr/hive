{
  super.tearDown();
  for (  TSocket socket : transports) {
    if (socket != null) {
      try {
        socket.close();
      }
 catch (      Exception e) {
      }
    }
  }
  if (server != null) {
    server.interrupt();
  }
}
