{
  boolean useDefaultRegion=HiveConf.getBoolVar(conf,HiveConf.ConfVars.HIVE_USE_INPUT_PRIMARY_REGION);
  if (!useDefaultRegion) {
    throw new SemanticException(ErrorMsg.USE_DEFAULT_REGION.getMsg());
  }
  this.ctx=ctx;
  analyzeInternal(ast);
}
