{
  try {
    tbl.setOwner(conf.getUser());
  }
 catch (  IOException e) {
    formatter.consoleError(console,"Unable to get current user: " + e.getMessage(),stringifyException(e),formatter.ERROR);
    return 1;
  }
  tbl.setCreateTime((int)(System.currentTimeMillis() / 1000));
  return 0;
}
