{
  String[] dbTab=fullName.split("\\.");
  String[] result=new String[2];
  if (dbTab.length == 2) {
    result[0]=dbTab[0];
    result[1]=dbTab[1];
  }
 else {
    result[0]=SessionState.get().getCurrentDatabase();
    result[1]=fullName;
  }
  return result;
}
