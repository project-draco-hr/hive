{
  HiveConf conf=new HiveConf(LineageHook.class);
  urlFactory=HookUtils.getUrlFactory(conf,FBHiveConf.CONNECTION_FACTORY,FBHiveConf.LINEAGE_CONNECTION_FACTORY,FBHiveConf.LINEAGE_MYSQL_TIER_VAR_NAME,FBHiveConf.LINEAGE_HOST_DATABASE_VAR_NAME);
}
