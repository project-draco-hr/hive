{
  if (this == o)   return true;
  if (o == null)   return false;
  if (o instanceof Sample) {
    Sample s=(Sample)o;
    return ((this.sampleNum == s.sampleNum) && (this.sampleFraction == s.sampleFraction) && this.sampleDimension.equals(s.sampleDimension));
  }
  return (false);
}
