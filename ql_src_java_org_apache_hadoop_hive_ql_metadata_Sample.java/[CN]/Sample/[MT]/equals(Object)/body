{
  if (this == o) {
    return true;
  }
  if (o == null) {
    return false;
  }
  if (o instanceof Sample) {
    Sample s=(Sample)o;
    return ((sampleNum == s.sampleNum) && (sampleFraction == s.sampleFraction) && sampleDimension.equals(s.sampleDimension));
  }
  return (false);
}
