{
  final int prime=31;
  int result=1;
  result=prime * result + ((sampleDimension == null) ? 0 : sampleDimension.hashCode());
  result=prime * result + sampleFraction;
  result=prime * result + sampleNum;
  return result;
}
