{
  Closer closer=Closer.create();
  try {
    closer.register(metadata);
    closer.register(dataReader);
    clearStreams();
  }
 catch (  IOException e) {
    throw closer.rethrow(e);
  }
 finally {
    closer.close();
  }
}
