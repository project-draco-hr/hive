{
  int pos;
  for (pos=0; pos < str.getLength() && pos < len; pos++) {
    data[pos]=txt[pos];
  }
  while (pos < len) {
    for (int i=0; i < pad.getLength() && i < len - pos; i++) {
      data[pos + i]=padTxt[i];
    }
    pos+=pad.getLength();
  }
}
