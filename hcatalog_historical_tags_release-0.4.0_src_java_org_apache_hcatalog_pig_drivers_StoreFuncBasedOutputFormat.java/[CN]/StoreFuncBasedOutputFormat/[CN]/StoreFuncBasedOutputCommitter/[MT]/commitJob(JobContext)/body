{
  wrappedOutputCommitter.commitJob(context);
  if (sf instanceof StoreMetadata) {
    if (rs != null) {
      ((StoreMetadata)sf).storeSchema(rs,location,new Job(context.getConfiguration()));
    }
  }
}
