{
  int count=0;
  if (!isAll) {
    for (    boolean b : rgsToRead) {
      if (b)       count++;
    }
  }
 else   if (!isNone) {
    count=rgCount;
  }
  counters.setCounter(LlapIOCounters.SELECTED_ROWGROUPS,count);
}
