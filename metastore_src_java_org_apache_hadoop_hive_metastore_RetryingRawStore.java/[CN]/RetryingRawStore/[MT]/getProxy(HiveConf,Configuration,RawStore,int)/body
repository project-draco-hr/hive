{
  RetryingRawStore handler=new RetryingRawStore(hiveConf,conf,base,id);
  return (RawStore)Proxy.newProxyInstance(RetryingRawStore.class.getClassLoader(),base.getClass().getInterfaces(),handler);
}
