{
  if (shouldUber) {
    if (tezWork.getChildren(work).isEmpty() && work instanceof ReduceWork && ((ReduceWork)work).getNumReduceTasks() == 1) {
      work.setUberMode(true);
    }
  }
  work.setLlapMode(true);
}
