{
  Preconditions.checkArgument(input == null,"AssertionError: MapInput doesn't take any input");
  return storageLevel == null || storageLevel.equals(StorageLevel.NONE()) ? hadoopRDD : hadoopRDD.mapToPair(new CopyFunction()).persist(storageLevel);
}
