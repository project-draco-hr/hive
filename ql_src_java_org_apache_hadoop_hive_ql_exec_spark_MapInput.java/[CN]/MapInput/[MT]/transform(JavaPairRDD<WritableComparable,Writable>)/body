{
  Preconditions.checkArgument(input == null,"AssertionError: MapInput doesn't take any input");
  return toCache ? hadoopRDD.mapToPair(new CopyFunction()).persist(StorageLevel.MEMORY_AND_DISK()) : hadoopRDD;
}
