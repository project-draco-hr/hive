{
  boolean r=removeSubQuery(root);
  if (r) {
    return root;
  }
  throw new SemanticException(ErrorMsg.UNSUPPORTED_SUBQUERY_EXPRESSION.getMsg(subQuery,"Only SubQuery expressions that are top level conjuncts are allowed"));
}
