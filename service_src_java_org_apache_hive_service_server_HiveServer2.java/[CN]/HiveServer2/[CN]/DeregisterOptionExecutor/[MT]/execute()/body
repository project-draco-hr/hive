{
  try {
    deleteServerInstancesFromZooKeeper(versionNumber);
  }
 catch (  Exception e) {
    LOG.fatal("Error deregistering HiveServer2 instances for version: " + versionNumber + " from ZooKeeper",e);
    System.exit(-1);
  }
  System.exit(0);
}
