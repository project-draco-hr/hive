{
  Preconditions.checkArgument(msgSize > 0,"Message size must be positive.");
  Preconditions.checkArgument(maxMessageSize <= 0 || msgSize <= maxMessageSize,"Message exceeds maximum allowed size (%s bytes).",maxMessageSize);
}
