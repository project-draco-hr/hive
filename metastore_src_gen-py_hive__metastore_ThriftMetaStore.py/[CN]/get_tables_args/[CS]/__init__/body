def __init__(self, d=None):
    self.db_name = None
    self.pattern = None
    if isinstance(d, dict):
        if ('db_name' in d):
            self.db_name = d['db_name']
        if ('pattern' in d):
            self.pattern = d['pattern']
