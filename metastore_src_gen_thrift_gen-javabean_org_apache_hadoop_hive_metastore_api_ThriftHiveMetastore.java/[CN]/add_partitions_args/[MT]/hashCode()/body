{
  List<Object> list=new ArrayList<Object>();
  boolean present_new_parts=true && (isSetNew_parts());
  list.add(present_new_parts);
  if (present_new_parts)   list.add(new_parts);
  return list.hashCode();
}
