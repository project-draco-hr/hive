{
switch (field) {
case TBL:
    if (value == null) {
      unsetTbl();
    }
 else {
      setTbl((Table)value);
    }
  break;
case ENVIRONMENT_CONTEXT:
if (value == null) {
  unsetEnvironment_context();
}
 else {
  setEnvironment_context((EnvironmentContext)value);
}
break;
}
}
