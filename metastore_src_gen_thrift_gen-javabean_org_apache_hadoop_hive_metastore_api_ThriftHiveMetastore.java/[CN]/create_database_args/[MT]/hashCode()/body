{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_database=true && (isSetDatabase());
  builder.append(present_database);
  if (present_database)   builder.append(database);
  return builder.toHashCode();
}
