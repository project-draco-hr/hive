{
  List<Object> list=new ArrayList<Object>();
  boolean present_database=true && (isSetDatabase());
  list.add(present_database);
  if (present_database)   list.add(database);
  return list.hashCode();
}
