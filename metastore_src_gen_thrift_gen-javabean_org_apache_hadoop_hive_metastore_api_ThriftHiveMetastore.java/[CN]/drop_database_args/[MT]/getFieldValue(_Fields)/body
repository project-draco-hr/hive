{
switch (field) {
case NAME:
    return getName();
case DELETE_DATA:
  return Boolean.valueOf(isDeleteData());
case CASCADE:
return Boolean.valueOf(isCascade());
}
throw new IllegalStateException();
}
