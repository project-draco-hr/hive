{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_name=true && (isSetName());
  builder.append(present_name);
  if (present_name)   builder.append(name);
  boolean present_deleteData=true;
  builder.append(present_deleteData);
  if (present_deleteData)   builder.append(deleteData);
  boolean present_cascade=true;
  builder.append(present_cascade);
  if (present_cascade)   builder.append(cascade);
  return builder.toHashCode();
}
