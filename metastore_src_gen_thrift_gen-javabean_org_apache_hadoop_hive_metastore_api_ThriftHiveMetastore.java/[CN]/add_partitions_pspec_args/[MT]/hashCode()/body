{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_new_parts=true && (isSetNew_parts());
  builder.append(present_new_parts);
  if (present_new_parts)   builder.append(new_parts);
  return builder.toHashCode();
}
