{
  List<Object> list=new ArrayList<Object>();
  boolean present_token_owner=true && (isSetToken_owner());
  list.add(present_token_owner);
  if (present_token_owner)   list.add(token_owner);
  boolean present_renewer_kerberos_principal_name=true && (isSetRenewer_kerberos_principal_name());
  list.add(present_renewer_kerberos_principal_name);
  if (present_renewer_kerberos_principal_name)   list.add(renewer_kerberos_principal_name);
  return list.hashCode();
}
