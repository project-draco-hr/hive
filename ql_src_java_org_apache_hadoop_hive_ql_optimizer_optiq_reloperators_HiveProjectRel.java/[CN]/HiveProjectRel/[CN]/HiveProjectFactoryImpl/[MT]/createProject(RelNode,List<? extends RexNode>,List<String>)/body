{
  RelOptCluster cluster=child.getCluster();
  RelDataType rowType=RexUtil.createStructType(cluster.getTypeFactory(),childExprs,fieldNames);
  RelNode project=HiveProjectRel.create(cluster,child,childExprs,rowType,child.getTraitSet(),Collections.<RelCollation>emptyList());
  return project;
}
