{
  int existing=_keys.indexOf(key);
  if (existing != -1) {
    Object before=_values.get(existing);
    _values.set(existing,value);
    return before;
  }
 else {
    _keys.add(key);
    _values.add(value);
    return null;
  }
}
