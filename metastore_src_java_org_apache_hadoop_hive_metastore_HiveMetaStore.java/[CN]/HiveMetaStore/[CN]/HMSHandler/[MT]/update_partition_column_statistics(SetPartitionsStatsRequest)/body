{
  boolean ret=false;
  try {
    ret=getMS().updatePartitionColumnStatistics(request);
    return ret;
  }
  finally {
    endFunction("write_partition_column_statistics: ",ret != false,null,null);
  }
}
