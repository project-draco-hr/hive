{
switch (rqst.getEventType()) {
case INSERT:
    InsertEvent event=new InsertEvent(rqst.getDbName(),rqst.getTableName(),rqst.getPartitionVals(),rqst.isSuccessful(),this);
  for (  MetaStoreEventListener listener : listeners) {
    listener.onInsert(event);
  }
break;
default :
throw new TException("Event type " + rqst.getEventType().toString() + " not currently "+ "supported.");
}
}
