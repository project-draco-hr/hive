{
  startFunction("add_partition");
  if (parts.size() == 0) {
    return 0;
  }
  Integer ret=null;
  try {
    ret=add_partitions_core(getMS(),parts);
  }
  finally {
    endFunction("add_partition",ret != null);
  }
  return ret;
}
