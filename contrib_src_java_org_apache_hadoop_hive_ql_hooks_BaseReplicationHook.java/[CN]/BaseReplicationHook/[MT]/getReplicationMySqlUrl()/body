{
  HiveConf conf=new HiveConf(BaseReplicationHook.class);
  return HookUtils.getUrlFactory(conf,FBHiveConf.CONNECTION_FACTORY,FBHiveConf.REPLICATION_CONNECTION_FACTORY,FBHiveConf.REPLICATION_MYSQL_TIER_VAR_NAME,FBHiveConf.REPLICATION_HOST_DATABASE_VAR_NAME);
}
