{
  StringBuilder sb=new StringBuilder("TDoubleValue(");
  boolean first=true;
  if (isSetValue()) {
    sb.append("value:");
    sb.append(this.value);
    first=false;
  }
  sb.append(")");
  return sb.toString();
}
