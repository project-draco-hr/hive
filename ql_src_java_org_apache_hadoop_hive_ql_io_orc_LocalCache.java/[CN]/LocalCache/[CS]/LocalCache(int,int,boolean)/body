{
  CacheBuilder builder=CacheBuilder.newBuilder().initialCapacity(DEFAULT_CACHE_INITIAL_CAPACITY).concurrencyLevel(numThreads).maximumSize(cacheStripeDetailsSize);
  if (useSoftRef) {
    builder=builder.softValues();
  }
  cache=builder.build();
}
