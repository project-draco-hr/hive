{
  super(iface);
  this.iface=iface;
  Method map=Processor.class.getDeclaredMethod("getProcessMap",Map.class);
  map.setAccessible(true);
  this.functions=(Map<String,ProcessFunction<I,? extends TBase<?,?>>>)map.invoke(null,new HashMap<String,ProcessFunction<I,? extends TBase<?,?>>>());
  shim=ShimLoader.getHadoopShims();
}
