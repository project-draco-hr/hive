{
  for (int i=0; i < batchSize; ++i) {
    if (!isNull[i]) {
      double value=vector[i];
      if (myagg.isNull) {
        myagg.value=value;
        myagg.isNull=false;
      }
 else       if (value > myagg.value) {
        myagg.value=value;
      }
    }
  }
}
