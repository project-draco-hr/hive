{
  opStack.push(nd);
  dispatch(nd,opStack);
  if (retMap.get(nd) != null) {
    opStack.pop();
    return;
  }
  if (nd.getChildren() != null) {
    for (    Node n : nd.getChildren()) {
      walk(n);
    }
  }
  opStack.pop();
}
