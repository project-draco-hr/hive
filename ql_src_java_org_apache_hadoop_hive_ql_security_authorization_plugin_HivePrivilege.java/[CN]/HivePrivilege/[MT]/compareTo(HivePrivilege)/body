{
  int compare=columns != null ? (privilege.columns != null ? compare(columns,privilege.columns) : 1) : (privilege.columns != null ? -1 : 0);
  if (compare == 0) {
    compare=name.compareTo(privilege.name);
  }
  return compare;
}
