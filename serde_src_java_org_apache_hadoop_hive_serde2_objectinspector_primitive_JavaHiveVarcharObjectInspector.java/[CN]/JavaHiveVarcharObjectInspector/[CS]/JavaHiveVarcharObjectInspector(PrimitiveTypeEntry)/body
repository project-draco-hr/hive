{
  super(typeEntry);
  if (typeEntry.primitiveCategory != PrimitiveCategory.VARCHAR) {
    throw new RuntimeException("TypeEntry of type varchar expected, got " + typeEntry.primitiveCategory);
  }
}
