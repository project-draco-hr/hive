{
  HiveVarchar setValue=(HiveVarchar)o;
  if (VarcharUtils.doesPrimitiveMatchTypeParams(value,(VarcharTypeInfo)typeInfo)) {
    setValue.setValue(value);
  }
 else {
    setValue.setValue(value,getMaxLength());
  }
  return setValue;
}
