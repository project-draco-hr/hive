{
switch (field) {
case LAST_EVENT:
    if (value == null) {
      unsetLastEvent();
    }
 else {
      setLastEvent((Long)value);
    }
  break;
case MAX_EVENTS:
if (value == null) {
  unsetMaxEvents();
}
 else {
  setMaxEvents((Integer)value);
}
break;
}
}
