{
  ArrayList<Object> signature=new ArrayList<Object>(1);
  signature.add(unionFieldObjectInspectors);
  LazyBinaryUnionObjectInspector result=cachedLazyBinaryUnionObjectInspector.get(signature);
  if (result == null) {
    result=new LazyBinaryUnionObjectInspector(unionFieldObjectInspectors);
    LazyBinaryUnionObjectInspector prev=cachedLazyBinaryUnionObjectInspector.putIfAbsent(signature,result);
    if (prev != null) {
      result=prev;
    }
  }
  return result;
}
