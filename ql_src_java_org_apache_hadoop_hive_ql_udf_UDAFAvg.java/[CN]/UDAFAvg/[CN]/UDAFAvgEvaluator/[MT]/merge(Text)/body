{
  if (o != null) {
    String s=o.toString();
    int pos=s.indexOf('/');
    assert(pos != -1);
    mSum+=Double.parseDouble(s.substring(0,pos));
    mCount+=Long.parseLong(s.substring(pos + 1));
  }
  return true;
}
