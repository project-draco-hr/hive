{
  if (other == null) {
    return 1;
  }
  return ComparisonChain.start().compare(roleName,other.roleName).compare(createTime,other.createTime).compare(principalName,other.principalName).compare(principalType,other.principalType).compare(grantOption,other.grantOption).compare(grantTime,other.grantTime).compare(grantor,other.grantor).result();
}
