{
  if (fiveScale == 0) {
    return;
  }
  if (fiveScale < 0) {
    throw new IllegalArgumentException();
  }
  if (isZero()) {
    return;
  }
  scaleDownFiveArrayRoundUp(v,fiveScale);
  updateCount();
}
