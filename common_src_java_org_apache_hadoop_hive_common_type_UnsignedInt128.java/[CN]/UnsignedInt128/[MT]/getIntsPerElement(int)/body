{
  assert(precision >= 0 && precision <= 38);
  if (precision <= 9) {
    return 1;
  }
 else   if (precision <= 19) {
    return 2;
  }
 else   if (precision <= 28) {
    return 3;
  }
  return 4;
}
