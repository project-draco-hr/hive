{
  try {
    return getDatabaseInternal(name);
  }
 catch (  MetaException e) {
    LOG.warn("Got a MetaException trying to call getDatabase(" + name + "), returning NoSuchObjectException",e);
    throw new NoSuchObjectException(e.getMessage());
  }
}
