{
  boolean success=false;
  try {
    openTransaction();
    createTable(tbl);
    addPrimaryKeys(primaryKeys);
    addForeignKeys(foreignKeys);
    success=commitTransaction();
  }
  finally {
    if (!success) {
      rollbackTransaction();
    }
  }
}
