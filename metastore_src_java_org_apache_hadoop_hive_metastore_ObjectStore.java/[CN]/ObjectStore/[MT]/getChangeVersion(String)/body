{
  boolean commited=false;
  try {
    openTransaction();
    long result=getMChangeVersionNoTx(topic).getVersion();
    commited=commitTransaction();
    return result;
  }
  finally {
    if (!commited) {
      rollbackTransaction();
    }
  }
}
