{
  acquire();
  try {
    MetadataOperation operation=getOperationManager().newGetTablesOperation(getSession(),catalogName,schemaName,tableName,tableTypes);
    operation.run();
    return operation.getHandle();
  }
  finally {
    release();
  }
}
