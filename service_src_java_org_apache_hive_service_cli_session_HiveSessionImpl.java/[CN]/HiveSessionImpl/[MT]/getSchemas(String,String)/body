{
  acquire();
  try {
    GetSchemasOperation operation=getOperationManager().newGetSchemasOperation(getSession(),catalogName,schemaName);
    operation.run();
    OperationHandle opHandle=operation.getHandle();
    opHandleSet.add(opHandle);
    return opHandle;
  }
  finally {
    release();
  }
}
