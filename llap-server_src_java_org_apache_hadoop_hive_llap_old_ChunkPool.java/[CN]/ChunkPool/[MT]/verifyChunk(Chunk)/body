{
  Chunk chunk=entry;
  while (chunk != null) {
    if (!chunk.buffer.lock(false))     break;
    chunk=chunk.nextChunk;
  }
  Chunk chunk2=entry;
  while (chunk2 != chunk) {
    chunk2.buffer.unlock();
    chunk2=chunk2.nextChunk;
  }
  return chunk == null;
}
