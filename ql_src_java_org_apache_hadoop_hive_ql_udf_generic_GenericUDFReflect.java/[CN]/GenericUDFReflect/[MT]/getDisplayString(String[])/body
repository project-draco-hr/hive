{
  StringBuilder sb=new StringBuilder();
  sb.append("reflect(");
  for (int i=0; i < children.length; i++) {
    if (i > 0) {
      sb.append(',');
    }
    sb.append(children[i]);
  }
  sb.append(')');
  return sb.toString();
}
