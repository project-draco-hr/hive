{
  return new SqlSplittableAggFunction.Registry<E>(){
    @Override public int register(    E e){
      int i=list.indexOf(e);
      if (i < 0) {
        i=list.size();
        list.add(e);
      }
      return i;
    }
  }
;
}
