{
  if (bytesRefWritable == null)   throw new IllegalArgumentException("Can not assign null.");
  ensureCapacity(index + 1);
  bytesRefWritables[index]=bytesRefWritable;
  if (valid <= index)   valid=index + 1;
}
