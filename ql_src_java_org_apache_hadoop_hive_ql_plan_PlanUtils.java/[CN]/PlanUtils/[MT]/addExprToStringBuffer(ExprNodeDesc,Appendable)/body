{
  try {
    sb.append(expr.getExprString());
    sb.append(" (type: ");
    sb.append(expr.getTypeString());
    sb.append(")");
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
