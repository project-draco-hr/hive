{
  if (!HiveConf.getBoolVar(conf,HiveConf.ConfVars.CLIPRINTCURRENTDB)) {
    return "";
  }
  String currDb=getCurrentDatabase();
  if (currDb == null) {
    return "";
  }
  return " (" + currDb + ")";
}
