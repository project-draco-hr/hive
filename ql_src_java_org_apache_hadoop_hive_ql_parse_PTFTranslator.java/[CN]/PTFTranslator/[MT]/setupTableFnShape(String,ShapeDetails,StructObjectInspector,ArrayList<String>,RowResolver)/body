{
  if (fnName.equals(FunctionRegistry.NOOP_TABLE_FUNCTION) || fnName.equals(FunctionRegistry.NOOP_MAP_TABLE_FUNCTION)) {
    return setupShapeForNoop(inpShape,OI,columnNames,rr);
  }
  return setupShape(OI,columnNames,rr);
}
