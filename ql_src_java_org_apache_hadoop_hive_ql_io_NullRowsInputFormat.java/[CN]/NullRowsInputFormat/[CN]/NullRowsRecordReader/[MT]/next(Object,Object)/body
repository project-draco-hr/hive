{
  if (rbCtx != null) {
    if (counter >= MAX_ROW)     return false;
    makeNullVrb(value,MAX_ROW);
    counter=MAX_ROW;
    return true;
  }
 else   if (counter++ < MAX_ROW) {
    return true;
  }
  return false;
}
