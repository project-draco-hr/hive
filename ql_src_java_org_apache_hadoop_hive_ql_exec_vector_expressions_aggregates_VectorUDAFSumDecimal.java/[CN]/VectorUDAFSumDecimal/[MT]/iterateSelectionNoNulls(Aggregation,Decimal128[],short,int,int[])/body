{
  if (myagg.isNull) {
    myagg.sum.zeroClear();
    myagg.isNull=false;
  }
  for (int i=0; i < batchSize; ++i) {
    Decimal128 value=vector[selected[i]];
    myagg.sum.addDestructive(value,scale);
  }
}
