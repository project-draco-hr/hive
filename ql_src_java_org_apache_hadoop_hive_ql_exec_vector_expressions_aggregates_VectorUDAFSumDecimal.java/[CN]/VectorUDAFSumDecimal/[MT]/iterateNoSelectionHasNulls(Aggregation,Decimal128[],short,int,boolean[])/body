{
  for (int i=0; i < batchSize; ++i) {
    if (!isNull[i]) {
      Decimal128 value=vector[i];
      if (myagg.isNull) {
        myagg.sum.zeroClear();
        myagg.isNull=false;
      }
      myagg.sum.addDestructive(value,scale);
    }
  }
}
