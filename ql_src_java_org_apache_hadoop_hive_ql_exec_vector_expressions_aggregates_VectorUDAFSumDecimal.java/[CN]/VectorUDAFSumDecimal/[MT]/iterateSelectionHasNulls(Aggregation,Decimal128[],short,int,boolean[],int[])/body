{
  for (int j=0; j < batchSize; ++j) {
    int i=selected[j];
    if (!isNull[i]) {
      Decimal128 value=vector[i];
      if (myagg.isNull) {
        myagg.isNull=false;
        myagg.sum.zeroClear();
      }
      myagg.sum.addDestructive(value,scale);
    }
  }
}
