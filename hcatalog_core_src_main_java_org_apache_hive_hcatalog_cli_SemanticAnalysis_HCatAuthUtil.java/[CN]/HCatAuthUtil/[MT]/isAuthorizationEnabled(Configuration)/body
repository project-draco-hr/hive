{
  return HiveConf.getBoolVar(conf,HiveConf.ConfVars.HIVE_AUTHORIZATION_ENABLED) && HiveConf.getVar(conf,HiveConf.ConfVars.HIVE_AUTHORIZATION_MANAGER) == StorageBasedAuthorizationProvider.class.getName();
}
