{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_successful=true;
  builder.append(present_successful);
  if (present_successful)   builder.append(successful);
  boolean present_data=true && (isSetData());
  builder.append(present_data);
  if (present_data)   builder.append(data);
  boolean present_dbName=true && (isSetDbName());
  builder.append(present_dbName);
  if (present_dbName)   builder.append(dbName);
  boolean present_tableName=true && (isSetTableName());
  builder.append(present_tableName);
  if (present_tableName)   builder.append(tableName);
  boolean present_partitionVals=true && (isSetPartitionVals());
  builder.append(present_partitionVals);
  if (present_partitionVals)   builder.append(partitionVals);
  return builder.toHashCode();
}
