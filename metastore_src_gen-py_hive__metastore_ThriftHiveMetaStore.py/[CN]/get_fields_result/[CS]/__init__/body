def __init__(self, d=None):
    self.success = None
    self.ouch = None
    self.out2 = None
    if isinstance(d, dict):
        if ('success' in d):
            self.success = d['success']
        if ('ouch' in d):
            self.ouch = d['ouch']
        if ('out2' in d):
            self.out2 = d['out2']
