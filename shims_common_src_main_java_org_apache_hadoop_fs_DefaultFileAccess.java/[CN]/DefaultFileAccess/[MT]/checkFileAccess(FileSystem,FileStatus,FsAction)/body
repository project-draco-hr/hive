{
  UserGroupInformation currentUgi=ShimLoader.getHadoopShims().getUGIForConf(fs.getConf());
  DefaultFileAccess.checkFileAccess(fs,stat,action,ShimLoader.getHadoopShims().getShortUserName(currentUgi),Arrays.asList(currentUgi.getGroupNames()));
}
