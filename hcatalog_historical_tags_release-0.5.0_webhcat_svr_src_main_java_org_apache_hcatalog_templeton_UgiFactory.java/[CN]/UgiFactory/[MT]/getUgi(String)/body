{
  UserGroupInformation ugi=userUgiMap.get(user);
  if (ugi == null) {
    final UserGroupInformation newUgi=UserGroupInformation.createProxyUser(user,UserGroupInformation.getLoginUser());
    userUgiMap.putIfAbsent(user,newUgi);
    return userUgiMap.get(user);
  }
  return ugi;
}
