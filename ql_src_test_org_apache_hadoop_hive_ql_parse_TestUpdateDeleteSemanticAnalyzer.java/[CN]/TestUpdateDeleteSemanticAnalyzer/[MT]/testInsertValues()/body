{
  try {
    ReturnInfo rc=parseAndAnalyze("insert into table T values ('abc', 3), ('ghi', 5)","testInsertValues");
    LOG.info(explain((SemanticAnalyzer)rc.sem,rc.plan,rc.ast.dump()));
  }
  finally {
    cleanupTables();
  }
}
