{
  if (DebugUtils.isTraceLockingEnabled()) {
    LOG.info("Unlocking " + cc.buffer + " for the fetching thread"+ (isBacktracking ? "; backtracking" : ""));
  }
  cache.releaseBuffer(cc.buffer);
  cc.isReleased=true;
}
