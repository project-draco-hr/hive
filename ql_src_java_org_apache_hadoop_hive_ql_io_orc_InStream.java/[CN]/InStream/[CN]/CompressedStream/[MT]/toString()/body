{
  return "compressed stream " + name + " base: "+ base+ " offset: "+ offset+ " limit: "+ limit+ (uncompressed == null ? "" : " uncompressed: " + uncompressed.position() + " to "+ uncompressed.limit());
}
