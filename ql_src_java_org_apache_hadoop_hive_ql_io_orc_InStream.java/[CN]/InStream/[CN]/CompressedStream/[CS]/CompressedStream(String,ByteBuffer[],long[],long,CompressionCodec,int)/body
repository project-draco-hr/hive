{
  this.bytes=input;
  this.name=name;
  this.codec=codec;
  this.length=length;
  if (this.length > 0) {
    isDirect=this.bytes[0].isDirect();
  }
  this.offsets=offsets;
  this.bufferSize=bufferSize;
  currentOffset=0;
  currentRange=0;
}
