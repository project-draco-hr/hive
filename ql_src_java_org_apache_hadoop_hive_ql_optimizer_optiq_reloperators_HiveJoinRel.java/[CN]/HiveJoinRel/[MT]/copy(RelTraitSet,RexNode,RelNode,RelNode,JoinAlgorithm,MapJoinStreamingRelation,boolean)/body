{
  try {
    return new HiveJoinRel(getCluster(),traitSet,left,right,conditionExpr,joinType,variablesStopped,joinalgo,streamingSide,semiJoinDone);
  }
 catch (  InvalidRelException e) {
    throw new AssertionError(e);
  }
}
