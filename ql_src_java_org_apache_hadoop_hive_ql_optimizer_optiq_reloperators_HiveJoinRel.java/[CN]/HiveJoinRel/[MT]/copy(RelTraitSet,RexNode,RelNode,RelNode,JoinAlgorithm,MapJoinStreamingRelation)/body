{
  try {
    return new HiveJoinRel(getCluster(),traitSet,left,right,conditionExpr,joinType,variablesStopped,joinalgo,streamingSide,this.m_leftSemiJoin);
  }
 catch (  InvalidRelException e) {
    throw new AssertionError(e);
  }
}
