{
  try {
    Set<String> variablesStopped=Collections.emptySet();
    return new HiveJoinRel(cluster,null,left,right,condition,joinType,variablesStopped,JoinAlgorithm.NONE,null);
  }
 catch (  InvalidRelException e) {
    throw new RuntimeException(e);
  }
}
