{
  try {
    Set<String> variablesStopped=Collections.emptySet();
    return new HiveJoinRel(getCluster(),traitSet,left,right,conditionExpr,joinType,variablesStopped,JoinAlgorithm.NONE,null,m_leftSemiJoin);
  }
 catch (  InvalidRelException e) {
    throw new AssertionError(e);
  }
}
