{
  if (buffer == listTail) {
    listTail=buffer.prev;
  }
 else {
    buffer.next.prev=buffer.prev;
  }
  if (buffer == listHead) {
    listHead=buffer.next;
  }
 else {
    buffer.prev.next=buffer.next;
  }
}
