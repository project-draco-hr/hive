{
  if (!inner_) {
    if (index_ < columns_.length) {
      fields_=columns_[index_++].split(new String(secondarySeparator_));
    }
 else {
      fields_=null;
    }
    innerIndex_=0;
  }
  TField f=new TField();
  f.type=-1;
  return f;
}
