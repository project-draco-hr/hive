{
  assert(children.length >= 2);
  StringBuilder sb=new StringBuilder();
  sb.append("(");
  boolean first=true;
  for (  String or : children) {
    if (!first) {
      sb.append(" or ");
    }
 else {
      first=false;
    }
    sb.append(or);
  }
  sb.append(")");
  return sb.toString();
}
