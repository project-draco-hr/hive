{
  for (int i=0; i < batchSize; ++i) {
    if (!isNull[i]) {
      long value=vector[i];
      if (myagg.isNull) {
        myagg.sum=0;
        myagg.isNull=false;
      }
      myagg.sum+=value;
    }
  }
}
