{
  super.write(obj);
  if (obj != null) {
    String val=((StringObjectInspector)inspector).getPrimitiveJavaObject(obj);
    rows.add(dictionary.add(val));
    indexStatistics.updateString(val);
  }
}
