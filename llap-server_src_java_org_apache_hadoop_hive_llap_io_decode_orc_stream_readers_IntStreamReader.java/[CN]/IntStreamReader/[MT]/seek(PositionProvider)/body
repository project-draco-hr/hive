{
  if (present != null) {
    if (isFileCompressed) {
      index.getNext();
    }
    present.seek(index);
  }
  if (reader != null && data.getStreamLength() > 0) {
    if (isFileCompressed) {
      index.getNext();
    }
    reader.seek(index);
  }
}
