{
  StringBuilder sb=new StringBuilder("GrantRevokeRoleResponse(");
  boolean first=true;
  if (isSetSuccess()) {
    sb.append("success:");
    sb.append(this.success);
    first=false;
  }
  sb.append(")");
  return sb.toString();
}
