{
  ObjectInspector oi=LazyFactory.createLazyObjectInspector(type,serdeParams.getSeparators(),1,serdeParams.getNullSequence(),serdeParams.isEscaped(),serdeParams.getEscapeChar(),ObjectInspectorOptions.AVRO);
  initInternalObjectInspectors(oi);
  return oi;
}
