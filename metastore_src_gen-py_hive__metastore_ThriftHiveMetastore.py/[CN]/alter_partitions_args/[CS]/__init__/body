def __init__(self, d=None):
    self.sd = None
    self.parts = None
    if isinstance(d, dict):
        if ('sd' in d):
            self.sd = d['sd']
        if ('parts' in d):
            self.parts = d['parts']
