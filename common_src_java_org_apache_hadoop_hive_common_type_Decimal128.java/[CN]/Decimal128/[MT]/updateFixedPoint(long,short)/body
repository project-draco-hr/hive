{
  this.scale=scale;
  if (val < 0L) {
    this.unscaledValue.update(-val);
    this.signum=-1;
  }
 else   if (val == 0L) {
    zeroClear();
  }
 else {
    this.unscaledValue.update(val);
    this.signum=1;
  }
}
