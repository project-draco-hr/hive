{
  this.scale=0;
  if (val < 0L) {
    this.unscaledValue.update(-val);
    this.signum=-1;
  }
 else   if (val == 0L) {
    zeroClear();
  }
 else {
    this.unscaledValue.update(val);
    this.signum=1;
  }
  if (scale != 0) {
    changeScaleDestructive(scale);
  }
}
