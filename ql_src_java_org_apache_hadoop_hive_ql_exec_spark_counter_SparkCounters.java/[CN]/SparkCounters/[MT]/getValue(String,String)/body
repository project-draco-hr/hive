{
  SparkCounter counter=getGroup(groupName).getCounter(counterName);
  if (counter == null) {
    throw new RuntimeException(String.format("counter[%s, %s] has not initialized before.",groupName,counterName));
  }
  return counter.getValue();
}
