{
  String[] head=header.values;
  String[] vals=row.values;
  beeLine.output("  <result>");
  for (int i=0; i < head.length && i < vals.length; i++) {
    beeLine.output("    <" + head[i] + ">"+ (BeeLine.xmlattrencode(vals[i]))+ "</"+ head[i]+ ">");
  }
  beeLine.output("  </result>");
}
