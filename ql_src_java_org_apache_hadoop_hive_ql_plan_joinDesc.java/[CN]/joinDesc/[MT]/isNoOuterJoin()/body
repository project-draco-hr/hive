{
  for (  org.apache.hadoop.hive.ql.plan.joinCond cond : conds) {
    if (cond.getType() == joinDesc.FULL_OUTER_JOIN || (cond.getType() == joinDesc.LEFT_OUTER_JOIN) || cond.getType() == joinDesc.RIGHT_OUTER_JOIN) {
      return false;
    }
  }
  return true;
}
