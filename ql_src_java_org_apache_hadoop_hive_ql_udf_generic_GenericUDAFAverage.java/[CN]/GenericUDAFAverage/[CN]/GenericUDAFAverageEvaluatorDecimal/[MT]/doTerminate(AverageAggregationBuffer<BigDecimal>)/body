{
  if (aggregation.count == 0) {
    return null;
  }
 else {
    BigDecimalWritable result=new BigDecimalWritable(BigDecimal.ZERO);
    result.set(aggregation.sum.divide(new BigDecimal(aggregation.count),UDFOPDivide.MAX_SCALE,RoundingMode.HALF_UP));
    return result;
  }
}
