{
  if (totalSeconds > 0 && nanos < 0) {
    --totalSeconds;
    nanos+=DateUtils.NANOS_PER_SEC;
  }
 else   if (totalSeconds < 0 && nanos > 0) {
    ++totalSeconds;
    nanos-=DateUtils.NANOS_PER_SEC;
  }
}
