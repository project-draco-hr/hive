{
  boolean inPlaceUpdates=HiveConf.getBoolVar(conf,HiveConf.ConfVars.TEZ_EXEC_INPLACE_PROGRESS);
  return inPlaceUpdates && !SessionState.getConsole().getIsSilent() && isUnixTerminal()&& TerminalFactory.get().getWidth() >= MIN_TERMINAL_WIDTH;
}
