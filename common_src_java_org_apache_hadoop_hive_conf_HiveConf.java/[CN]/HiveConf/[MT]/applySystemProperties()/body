{
  for (  ConfVars oneVar : ConfVars.values()) {
    if (System.getProperty(oneVar.varname) != null) {
      if (System.getProperty(oneVar.varname).length() > 0)       this.set(oneVar.varname,System.getProperty(oneVar.varname));
    }
  }
}
