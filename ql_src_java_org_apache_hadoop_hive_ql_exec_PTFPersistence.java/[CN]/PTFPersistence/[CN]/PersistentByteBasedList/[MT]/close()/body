{
  super.close();
  ByteBasedList list=memList.get();
  if (list != null) {
    list.close();
  }
  memList.clear();
  try {
    PartitionedByteBasedList.deleteRecursively(file);
  }
 catch (  Exception e) {
  }
}
