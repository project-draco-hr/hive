{
  if (container.rowCount() > 0 && cursor < container.rowCount()) {
    return true;
  }
  if (backupContainer == null || backupContainer.rowCount() == 0) {
    return false;
  }
  clear();
  RowContainer tmp=container;
  container=backupContainer;
  backupContainer=tmp;
  return true;
}
