{
  String command=SessionState.get() != null ? SessionState.get().getLastCommand() : null;
  Assert.fail("Client Execution was expected to fail, but succeeded with error code 0 " + (command != null ? " running " + command : "") + (debugHint != null ? debugHint : ""));
}
