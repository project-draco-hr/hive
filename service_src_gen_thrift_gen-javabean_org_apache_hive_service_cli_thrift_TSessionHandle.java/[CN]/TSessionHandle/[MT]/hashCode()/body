{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_sessionId=true && (isSetSessionId());
  builder.append(present_sessionId);
  if (present_sessionId)   builder.append(sessionId);
  return builder.toHashCode();
}
