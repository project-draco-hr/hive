{
  Set<Operator<?>> operators=new HashSet<Operator<?>>();
  for (  List<Operator<?>> works : values) {
    for (    Operator<?> work : works) {
      if (work != null) {
        operators.add(work);
      }
    }
  }
  return operators;
}
