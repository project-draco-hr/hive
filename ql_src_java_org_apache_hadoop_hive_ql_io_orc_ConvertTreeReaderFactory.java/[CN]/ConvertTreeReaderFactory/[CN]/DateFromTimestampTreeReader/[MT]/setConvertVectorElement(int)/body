{
  timestampResult.set(timestampColVector.asScratchTimestamp(elementNum));
  Date dateValue=DateWritable.timeToDate(timestampResult.getSeconds());
  longColVector.vector[elementNum]=DateWritable.dateToDays(dateValue);
}
