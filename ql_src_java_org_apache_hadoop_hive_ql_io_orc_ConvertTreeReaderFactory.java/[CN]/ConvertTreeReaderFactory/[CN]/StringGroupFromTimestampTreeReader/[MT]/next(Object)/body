{
  TimestampWritable readTimestampWritableResult=(TimestampWritable)timestampTreeReader.next(timestampWritableResult);
  Writable result=null;
  if (readTimestampWritableResult != null) {
    result=getStringGroupResultFromString(previous,readerType,readTimestampWritableResult.toString());
  }
  return result;
}
