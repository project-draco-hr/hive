{
  String stringValue=stringFromStringGroupTreeReader(stringGroupTreeReader,writable,fileType);
  DateWritable result=null;
  if (stringValue != null) {
    Date dateValue=parseDateFromString(stringValue);
    if (dateValue != null) {
      if (previous == null) {
        result=new DateWritable();
      }
 else {
        result=(DateWritable)previous;
      }
      result.set(dateValue);
    }
  }
  return result;
}
