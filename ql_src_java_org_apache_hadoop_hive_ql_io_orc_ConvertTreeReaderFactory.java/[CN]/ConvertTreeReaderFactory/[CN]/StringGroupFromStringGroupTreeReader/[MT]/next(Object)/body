{
  String stringValue=stringFromStringGroupTreeReader(stringGroupTreeReader,writable,fileType);
  Writable result=null;
  if (stringValue != null) {
    result=getStringGroupResultFromString(previous,readerType,stringValue);
  }
  return result;
}
