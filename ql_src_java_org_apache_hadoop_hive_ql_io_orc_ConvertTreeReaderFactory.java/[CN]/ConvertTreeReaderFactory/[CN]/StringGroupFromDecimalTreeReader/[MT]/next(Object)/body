{
  HiveDecimalWritable readHiveDecimalResult=(HiveDecimalWritable)decimalTreeReader.next(hiveDecimalResult);
  Writable result=null;
  if (readHiveDecimalResult != null) {
    result=getStringGroupResultFromString(previous,readerType,readHiveDecimalResult.getHiveDecimal().toString());
  }
  return result;
}
