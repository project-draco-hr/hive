{
  DateWritable readDateWritableResult=(DateWritable)dateTreeReader.next(dateWritableResult);
  Writable result=null;
  if (readDateWritableResult != null) {
    result=getStringGroupResultFromString(previous,readerType,readDateWritableResult.toString());
  }
  return result;
}
