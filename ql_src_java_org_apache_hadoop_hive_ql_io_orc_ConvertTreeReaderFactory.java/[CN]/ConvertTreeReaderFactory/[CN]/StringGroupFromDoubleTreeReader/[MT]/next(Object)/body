{
  DoubleWritable readDoubleResult=(DoubleWritable)doubleTreeReader.next(doubleResult);
  Writable result=null;
  if (readDoubleResult != null) {
    double doubleValue=readDoubleResult.get();
    if (!Double.isNaN(doubleValue)) {
      result=getStringGroupResultFromString(previous,readerType,String.valueOf(doubleValue));
    }
  }
  return result;
}
