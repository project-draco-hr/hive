{
  if (commonTypeInfo instanceof DecimalTypeInfo) {
    if ((!FunctionRegistry.isExactNumericType((PrimitiveTypeInfo)ti)) || (!FunctionRegistry.isExactNumericType((PrimitiveTypeInfo)returnType))) {
      return TypeInfoFactory.doubleTypeInfo;
    }
  }
  return commonTypeInfo;
}
