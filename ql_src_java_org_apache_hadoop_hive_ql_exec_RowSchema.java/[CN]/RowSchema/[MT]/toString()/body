{
  StringBuilder sb=new StringBuilder('(');
  if (signature != null) {
    for (    ColumnInfo col : signature) {
      sb.append(col.toString());
    }
  }
  sb.append(')');
  return sb.toString();
}
