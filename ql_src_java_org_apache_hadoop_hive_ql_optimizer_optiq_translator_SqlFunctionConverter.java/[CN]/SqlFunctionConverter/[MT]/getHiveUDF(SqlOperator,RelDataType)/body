{
  String name=reverseOperatorMap.get(op);
  FunctionInfo hFn=name != null ? FunctionRegistry.getFunctionInfo(name) : null;
  if (hFn == null)   hFn=handleExplicitCast(op,dt);
  return hFn == null ? null : hFn.getGenericUDF();
}
