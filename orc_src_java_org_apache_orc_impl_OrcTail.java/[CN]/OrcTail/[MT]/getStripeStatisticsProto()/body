{
  if (serializedTail == null)   return null;
  if (metadata == null) {
    metadata=extractMetadata(serializedTail,0,(int)fileTail.getPostscript().getMetadataLength(),getCompressionCodec(),getCompressionBufferSize());
    serializedTail.clear();
  }
  return metadata.getStripeStatsList();
}
