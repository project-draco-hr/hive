{
  StdAgg myagg=(StdAgg)agg;
  if (myagg.count == 0) {
    return null;
  }
 else {
    if (myagg.count > 1) {
      result.set(myagg.variance / (myagg.count - 1));
    }
 else {
      result.set(0);
    }
    return result;
  }
}
