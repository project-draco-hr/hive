{
  if (datestring == null) {
    Date date=new Date();
    return date.getTime() / 1000;
  }
  try {
    Date date=(Date)formatter.parse(datestring);
    return date.getTime() / 1000;
  }
 catch (  ParseException e) {
    return 0;
  }
}
