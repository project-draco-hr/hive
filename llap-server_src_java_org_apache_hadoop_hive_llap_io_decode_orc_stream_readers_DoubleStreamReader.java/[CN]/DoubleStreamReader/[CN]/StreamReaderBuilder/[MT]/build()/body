{
  InStream present=StreamUtils.createInStream(OrcProto.Stream.Kind.PRESENT.name(),fileName,null,bufferSize,presentStream);
  InStream data=StreamUtils.createInStream(OrcProto.Stream.Kind.DATA.name(),fileName,null,bufferSize,dataStream);
  boolean isFileCompressed=compressionCodec != null;
  return new DoubleStreamReader(columnIndex,present,data,isFileCompressed,rowIndex);
}
