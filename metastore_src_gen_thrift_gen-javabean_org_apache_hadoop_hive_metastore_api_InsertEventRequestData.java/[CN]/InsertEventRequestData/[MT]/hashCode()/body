{
  List<Object> list=new ArrayList<Object>();
  boolean present_filesAdded=true && (isSetFilesAdded());
  list.add(present_filesAdded);
  if (present_filesAdded)   list.add(filesAdded);
  return list.hashCode();
}
