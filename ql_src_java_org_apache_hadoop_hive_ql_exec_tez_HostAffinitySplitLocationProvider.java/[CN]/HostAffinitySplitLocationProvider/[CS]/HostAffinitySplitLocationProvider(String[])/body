{
  Preconditions.checkState(knownLocations != null && knownLocations.length != 0,HostAffinitySplitLocationProvider.class.getName() + "needs at least 1 location to function");
  this.knownLocations=knownLocations;
}
