{
  for (int j=0; j < batchSize; ++j) {
    int i=selected[j];
    if (!isNull[i]) {
      long value=vector[i];
      if (myagg.isNull) {
        myagg.isNull=false;
        myagg.value=value;
      }
 else       if (value > myagg.value) {
        myagg.value=value;
      }
    }
  }
}
