{
  if (getOrder() == null) {
    OrderSpec order=new OrderSpec();
    order.prefixBy(getPartition());
    if (wFn.isDistinct) {
      order.addExpressions(wFn.getArgs());
    }
    setOrder(order);
  }
}
