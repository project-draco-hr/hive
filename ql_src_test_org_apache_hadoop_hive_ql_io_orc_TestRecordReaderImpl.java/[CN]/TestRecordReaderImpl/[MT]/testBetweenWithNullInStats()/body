{
  List<Object> args=new ArrayList<Object>();
  args.add("c");
  args.add("f");
  PredicateLeaf pred=TestSearchArgumentImpl.createPredicateLeaf(PredicateLeaf.Operator.BETWEEN,PredicateLeaf.Type.STRING,"x",null,args);
  assertEquals(TruthValue.YES_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("d","e",true),pred));
  assertEquals(TruthValue.YES_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("e","f",true),pred));
  assertEquals(TruthValue.NO_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("h","g",true),pred));
  assertEquals(TruthValue.YES_NO_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("f","g",true),pred));
  assertEquals(TruthValue.YES_NO_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("e","g",true),pred));
  assertEquals(TruthValue.YES_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("c","e",true),pred));
  assertEquals(TruthValue.YES_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("c","f",true),pred));
  assertEquals(TruthValue.YES_NO_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("c","g",true),pred));
  assertEquals(TruthValue.NO_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("a","b",true),pred));
  assertEquals(TruthValue.YES_NO_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("a","c",true),pred));
  assertEquals(TruthValue.YES_NO_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("b","d",true),pred));
  assertEquals(TruthValue.YES_NULL,RecordReaderImpl.evaluatePredicate(createStringStats("c","c",true),pred));
}
