{
  intervalDayTimeValues[index].set(colVector.asScratchIntervalDayTime(elementNum));
  isNull[longValues.length + doubleValues.length + byteValues.length+ decimalValues.length+ timestampValues.length+ index]=false;
}
