{
  for (int i=0; i < byteValues.length; ++i) {
    if (!isNull[longValues.length + doubleValues.length + i]) {
      if (0 != StringExpr.compare(byteValues[i],byteStarts[i],byteLengths[i],keyThat.byteValues[i],keyThat.byteStarts[i],keyThat.byteLengths[i])) {
        return false;
      }
    }
  }
  return true;
}
