{
  boolean validParent=true;
  if (parent != null && !(parent instanceof Project) && !((parent instanceof Sort) || HiveCalciteUtil.orderRelNode(parent)))   validParent=false;
  return validParent;
}
