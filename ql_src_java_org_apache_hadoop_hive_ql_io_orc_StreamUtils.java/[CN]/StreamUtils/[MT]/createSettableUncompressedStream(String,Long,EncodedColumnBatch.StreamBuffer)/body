{
  if (streamBuffer == null) {
    return null;
  }
  DiskRangeInfo diskRangeInfo=createDiskRangeInfo(streamBuffer);
  return new SettableUncompressedStream(fileId,streamName,diskRangeInfo.getDiskRanges(),diskRangeInfo.getTotalLength());
}
