{
  if (outWriter == null) {
    return;
  }
  try {
    if (fdis != null) {
      fdis.close();
      fdis=null;
    }
    outWriter.close();
    outWriter=null;
  }
 catch (  IOException e) {
    throw new HiveException("Unable to close OrcFileMergeOperator",e);
  }
  super.closeOp(abort);
}
