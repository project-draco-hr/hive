{
  try {
    if (fdis != null) {
      fdis.close();
      fdis=null;
    }
    if (outWriter != null) {
      outWriter.close();
      outWriter=null;
    }
  }
 catch (  Exception e) {
    throw new HiveException("Unable to close OrcFileMergeOperator",e);
  }
  super.closeOp(abort);
}
