{
  System.out.println("Evaluating " + expr + " for "+ times+ " times");
  InspectableObject output=new InspectableObject();
  long start=System.currentTimeMillis();
  for (int i=0; i < times; i++) {
    eval.evaluate(input.o,input.oi,output);
  }
  long end=System.currentTimeMillis();
  assertEquals(standardJavaOutput,ObjectInspectorUtils.copyToStandardObject(output.o,output.oi,ObjectInspectorCopyOption.JAVA));
  System.out.println("Evaluation finished: " + String.format("%2.3f",(end - start) * 0.001) + " seconds, "+ String.format("%2.3f",(end - start) * 1000.0 / times)+ " seconds/million call.");
}
