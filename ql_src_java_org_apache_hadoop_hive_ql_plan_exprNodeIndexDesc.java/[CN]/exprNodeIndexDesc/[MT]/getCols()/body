{
  List<String> colList=new ArrayList<String>();
  if (desc != null)   colList=Utilities.mergeUniqElems(colList,desc.getCols());
  if (index != null)   colList=Utilities.mergeUniqElems(colList,index.getCols());
  return colList;
}
