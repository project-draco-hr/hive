{
  super(desc.getTypeInfo().getCategory().equals(Category.LIST) ? ((ListTypeInfo)desc.getTypeInfo()).getListElementTypeInfo() : ((MapTypeInfo)desc.getTypeInfo()).getMapValueTypeInfo());
  this.desc=desc;
  this.index=index;
}
