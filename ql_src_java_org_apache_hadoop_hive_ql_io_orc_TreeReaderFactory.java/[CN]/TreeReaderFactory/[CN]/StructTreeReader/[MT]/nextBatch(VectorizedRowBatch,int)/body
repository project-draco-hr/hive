{
  for (int i=0; i < fields.length && (vectorColumnCount == -1 || i < vectorColumnCount); ++i) {
    batch.cols[i].reset();
    batch.cols[i].ensureSize((int)batchSize,false);
    fields[i].nextVector(batch.cols[i],null,batchSize);
  }
}
