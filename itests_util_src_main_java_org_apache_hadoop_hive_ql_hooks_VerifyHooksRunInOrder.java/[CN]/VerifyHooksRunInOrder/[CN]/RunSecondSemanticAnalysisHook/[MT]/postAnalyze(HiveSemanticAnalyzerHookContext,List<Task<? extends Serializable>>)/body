{
  LogHelper console=SessionState.getConsole();
  if (console == null) {
    return;
  }
  console.printError("Running RunSecond for Post Analysis Hook");
  Assert.assertTrue("Post Analysis Hooks did not run in the order specified.",staticAnalysisPostHookFirstRan);
}
