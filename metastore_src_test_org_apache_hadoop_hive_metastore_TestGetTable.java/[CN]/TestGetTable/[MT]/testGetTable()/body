{
  try {
    String dbname="bar";
    String name="pete_ms_test2";
    DB db=DB.createDB(dbname,conf_);
    Table t=Table.create(db,name,createSchema(dbname,name),conf_);
    assertTrue(t.equals(db.getTable(name,false)));
    cleanup();
  }
 catch (  MetaException e) {
    e.printStackTrace();
    throw e;
  }
}
