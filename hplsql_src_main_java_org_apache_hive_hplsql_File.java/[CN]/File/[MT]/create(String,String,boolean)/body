{
  path=new Path(dir,file);
  try {
    if (fs == null) {
      fs=FileSystem.get(new Configuration());
    }
    out=fs.create(path,overwrite);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
