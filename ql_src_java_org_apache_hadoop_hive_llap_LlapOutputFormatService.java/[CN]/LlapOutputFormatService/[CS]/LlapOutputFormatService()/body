{
  writers=new HashMap<String,RecordWriter>();
  conf=new HiveConf();
  executor=Executors.newSingleThreadExecutor(new ThreadFactoryBuilder().setDaemon(true).setNameFormat("LLAP output %d").build());
  socket=new ServerSocket(conf.getIntVar(HiveConf.ConfVars.LLAP_DAEMON_OUTPUT_SERVICE_PORT));
}
