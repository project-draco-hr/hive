{
  try {
    return baseSplit.getLength();
  }
 catch (  IOException e) {
    LOG.warn(e.getMessage());
    LOG.warn(e.getStackTrace());
  }
catch (  InterruptedException e) {
    LOG.warn(e.getMessage());
    LOG.warn(e.getStackTrace());
  }
  return 0;
}
