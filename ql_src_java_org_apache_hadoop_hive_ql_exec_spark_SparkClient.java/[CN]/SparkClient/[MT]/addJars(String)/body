{
  for (  String addedJar : addedJars.split(",")) {
    if (StringUtils.isNotEmpty(addedJar) && StringUtils.isNotBlank(addedJar) && !localJars.contains(addedJar)) {
      localJars.add(addedJar);
      sc.addJar(addedJar);
    }
  }
}
