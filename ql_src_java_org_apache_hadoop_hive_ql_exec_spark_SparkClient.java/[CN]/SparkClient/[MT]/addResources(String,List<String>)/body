{
  for (  String addedFile : addedFiles.split(",")) {
    if (StringUtils.isNotEmpty(addedFile) && StringUtils.isNotBlank(addedFile) && !localCache.contains(addedFile)) {
      localCache.add(addedFile);
      sc.addFile(addedFile);
    }
  }
}
