{
  if ((a == null) || (b == null)) {
    return null;
  }
  if (b.getHiveDecimal().compareTo(HiveDecimal.ZERO) == 0) {
    return null;
  }
 else {
    decimalWritable.set(a.getHiveDecimal().divide(b.getHiveDecimal()));
  }
  return decimalWritable;
}
