{
  if ((a == null) || (b == null)) {
    return null;
  }
  if (b.getBigDecimal().compareTo(BigDecimal.ZERO) == 0) {
    return null;
  }
 else {
    bigDecimalWritable.set(a.getBigDecimal().divide(b.getBigDecimal(),MAX_SCALE,RoundingMode.HALF_UP));
  }
  return bigDecimalWritable;
}
