{
  try {
    ColumnStatistics colStats=Hive.get().getPartitionColumnStatistics(dbName,tabName,partName,colName);
    if (colStats != null) {
      return getColStatistics(colStats.getStatsObj().get(0),tabName,colName);
    }
  }
 catch (  HiveException e) {
    return null;
  }
  return null;
}
