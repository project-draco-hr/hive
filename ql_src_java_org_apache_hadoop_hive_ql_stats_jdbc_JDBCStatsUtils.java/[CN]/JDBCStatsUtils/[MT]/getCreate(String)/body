{
  String create="CREATE TABLE /* " + comment + " */ "+ JDBCStatsUtils.getStatTableName()+ " ("+ JDBCStatsUtils.getIdColumnName()+ " VARCHAR(255) PRIMARY KEY ";
  for (int i=0; i < supportedStats.size(); i++) {
    create+=", " + getStatColumnName(supportedStats.get(i)) + " BIGINT ";
  }
  create+=")";
  return create;
}
