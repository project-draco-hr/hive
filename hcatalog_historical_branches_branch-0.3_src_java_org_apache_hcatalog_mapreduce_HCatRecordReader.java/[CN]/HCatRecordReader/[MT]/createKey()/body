{
  WritableComparable o=null;
  try {
    o=getCurrentKey();
  }
 catch (  IOException e) {
    LOG.warn(e.getMessage());
    LOG.warn(e.getStackTrace());
  }
catch (  InterruptedException e) {
    LOG.warn(e.getMessage());
    LOG.warn(e.getStackTrace());
  }
  return o;
}
