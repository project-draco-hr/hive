{
  Path[] paths=CombineFileInputFormat.getInputPaths(conf);
  Path[] newPaths=new Path[paths.length];
  for (int pos=0; pos < paths.length; pos++)   newPaths[pos]=new Path(paths[pos].toString().substring(5));
  return newPaths;
}
