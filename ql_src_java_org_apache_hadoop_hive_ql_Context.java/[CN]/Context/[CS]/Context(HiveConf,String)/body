{
  this.conf=conf;
  this.executionId=executionId;
  localScratchPath=System.getProperty("java.io.tmpdir") + Path.SEPARATOR + System.getProperty("user.name")+ Path.SEPARATOR+ executionId;
  queryScratchPath=new Path(conf.getVar(HiveConf.ConfVars.SCRATCHDIR),executionId);
}
