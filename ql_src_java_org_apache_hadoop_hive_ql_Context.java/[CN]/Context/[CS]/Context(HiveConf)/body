{
  try {
    fs=FileSystem.get(conf);
    initialized=false;
    resDir=null;
    resFile=null;
  }
 catch (  IOException e) {
    LOG.info("Context creation error: " + StringUtils.stringifyException(e));
  }
}
