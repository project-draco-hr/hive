{
  Path dir=FileUtils.makeQualified(new Path(conf.getVar(HiveConf.ConfVars.SCRATCHDIR),executionId),conf);
  if (mkdir) {
    FileSystem fs=dir.getFileSystem(conf);
    if (!fs.mkdirs(dir)) {
      throw new IOException("Cannot make directory: " + dir);
    }
  }
  return dir;
}
