{
  Path dir=new Path(extURI.getScheme(),extURI.getAuthority(),queryScratchPath.toUri().getPath());
  if (mkdir) {
    FileSystem fs=dir.getFileSystem(conf);
    if (!fs.mkdirs(dir)) {
      throw new IOException("Cannot make directory: " + dir);
    }
  }
  return dir;
}
