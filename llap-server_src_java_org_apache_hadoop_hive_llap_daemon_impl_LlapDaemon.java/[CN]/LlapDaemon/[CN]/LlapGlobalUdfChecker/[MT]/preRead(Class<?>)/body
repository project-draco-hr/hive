{
  if (GenericUDFBridge.class == type)   return true;
  if (!(GenericUDF.class.isAssignableFrom(type) || UDF.class.isAssignableFrom(type)) || fnLocalizer.isUdfAllowed(type))   return false;
  throw new SecurityException("UDF " + type.getCanonicalName() + " is not allowed");
}
