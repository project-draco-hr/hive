{
  if (DebugUtils.isTraceMttEnabled()) {
    LlapIoImpl.LOG.info("close called; closed " + isClosed + ", done "+ isDone+ ", err "+ pendingError+ ", pending "+ pendingData.size());
  }
  LlapIoImpl.LOG.info(counters);
  feedback.stop();
  rethrowErrorIfAny();
}
