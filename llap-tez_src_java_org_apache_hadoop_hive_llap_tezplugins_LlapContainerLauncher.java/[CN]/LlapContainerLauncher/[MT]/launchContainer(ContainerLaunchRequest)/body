{
  LOG.info("No-op launch for container: " + containerLaunchRequest.getContainerId() + " succeeded on host: "+ containerLaunchRequest.getNodeId());
  getContext().containerLaunched(containerLaunchRequest.getContainerId());
}
