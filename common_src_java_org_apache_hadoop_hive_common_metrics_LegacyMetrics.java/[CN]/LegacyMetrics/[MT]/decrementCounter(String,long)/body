{
  Long value;
synchronized (metrics) {
    if (!metrics.hasKey(name)) {
      value=Long.valueOf(decrement);
      set(name,-value);
    }
 else {
      value=((Long)get(name)) - decrement;
      set(name,value);
    }
  }
  return value;
}
