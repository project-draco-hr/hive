{
  JobConf jobConf=cloneJobConf(mapWork);
  Class ifClass=getInputFormat(jobConf,mapWork);
  JavaPairRDD<HiveKey,BytesWritable> hadoopRDD=sc.hadoopRDD(jobConf,ifClass,WritableComparable.class,Writable.class);
  return new MapInput(hadoopRDD);
}
