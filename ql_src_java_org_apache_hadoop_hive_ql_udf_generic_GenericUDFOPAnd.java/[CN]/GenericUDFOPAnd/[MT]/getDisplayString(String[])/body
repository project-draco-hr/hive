{
  assert(children.length >= 2);
  StringBuilder sb=new StringBuilder();
  sb.append("(");
  boolean first=true;
  for (  String and : children) {
    if (!first) {
      sb.append(" and ");
    }
 else {
      first=false;
    }
    sb.append(and);
  }
  sb.append(")");
  return sb.toString();
}
