{
  OrcSerdeRow serdeRow=(OrcSerdeRow)row;
  if (writer == null) {
    writer=OrcFile.createWriter(fs,path,serdeRow.getInspector(),stripeSize,compress,compressionSize,rowIndexStride);
  }
  writer.addRow(serdeRow.getRow());
}
