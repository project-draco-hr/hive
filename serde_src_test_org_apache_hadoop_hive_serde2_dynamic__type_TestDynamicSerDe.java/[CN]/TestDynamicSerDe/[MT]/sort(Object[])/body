{
  for (int i=0; i < structs.length; i++)   for (int j=i + 1; j < structs.length; j++)   if (compare(structs[i],structs[j]) > 0) {
    Object t=structs[i];
    structs[i]=structs[j];
    structs[j]=t;
  }
}
