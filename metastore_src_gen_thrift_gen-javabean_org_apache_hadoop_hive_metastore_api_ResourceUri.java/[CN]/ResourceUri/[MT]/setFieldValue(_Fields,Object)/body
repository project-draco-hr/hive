{
switch (field) {
case RESOURCE_TYPE:
    if (value == null) {
      unsetResourceType();
    }
 else {
      setResourceType((ResourceType)value);
    }
  break;
case URI:
if (value == null) {
  unsetUri();
}
 else {
  setUri((String)value);
}
break;
}
}
