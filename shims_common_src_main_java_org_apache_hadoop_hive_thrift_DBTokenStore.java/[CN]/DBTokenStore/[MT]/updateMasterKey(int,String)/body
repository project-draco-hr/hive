{
  if (LOG.isTraceEnabled()) {
    LOG.trace("updateMasterKey: s = " + s + ", keySeq = "+ keySeq);
  }
  invokeOnTokenStore("updateMasterKey",new Object[]{Integer.valueOf(keySeq),s},Integer.class,String.class);
}
