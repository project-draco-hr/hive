{
  if (nanoOfDay >= NANOSECONDS_PER_DAY || nanoOfDay <= -NANOSECONDS_PER_DAY) {
    return false;
  }
  if (!(epochDay >= 0 && nanoOfDay >= 0 || epochDay <= 0 && nanoOfDay <= 0)) {
    return false;
  }
  return true;
}
