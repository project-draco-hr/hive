{
  Preconditions.checkState(validateIntegrity(epochDay,nanoOfDay),"epochDay " + epochDay + ", nanoOfDay "+ nanoOfDay+ " not valid");
  this.epochDay=epochDay;
  this.nanoOfDay=nanoOfDay;
  scratchTimestamp=new Timestamp(0);
}
