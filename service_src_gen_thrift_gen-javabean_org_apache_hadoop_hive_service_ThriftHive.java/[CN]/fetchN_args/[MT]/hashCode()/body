{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_numRows=true;
  builder.append(present_numRows);
  if (present_numRows)   builder.append(numRows);
  return builder.toHashCode();
}
