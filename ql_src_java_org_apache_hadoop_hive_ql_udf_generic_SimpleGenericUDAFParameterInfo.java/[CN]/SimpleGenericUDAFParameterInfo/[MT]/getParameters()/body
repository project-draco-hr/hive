{
  TypeInfo[] result=new TypeInfo[parameters.length];
  for (int ii=0; ii < parameters.length; ++ii) {
    result[ii]=TypeInfoUtils.getTypeInfoFromObjectInspector(parameters[ii]);
  }
  return result;
}
