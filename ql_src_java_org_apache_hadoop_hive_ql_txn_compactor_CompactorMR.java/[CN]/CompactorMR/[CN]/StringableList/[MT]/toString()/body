{
  StringBuffer buf=new StringBuffer();
  buf.append(size());
  buf.append(':');
  if (size() > 0) {
    for (    Path p : this) {
      buf.append(p.toString().length());
      buf.append(':');
      buf.append(p.toString());
    }
  }
  return buf.toString();
}
