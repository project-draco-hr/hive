{
  try {
    console.printError(msg,detail);
    asJson(console.getOutStream(),MapBuilder.create().put("error",msg).put("errorDetail",detail).put("errorCode",errorCode).build());
  }
 catch (  HiveException e) {
    console.printError("unable to create json: " + e);
  }
}
