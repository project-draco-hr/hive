{
  n=(int)(expectedSize * PADDING_FACTOR / 2.0);
  int primes[]=CuckooSetLong.primes;
  for (int i=0; i != primes.length; i++) {
    if (n <= primes[i]) {
      n=primes[i];
      break;
    }
  }
  t1=new byte[n][];
  t2=new byte[n][];
  updateHashSalt();
}
