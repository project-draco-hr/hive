{
  createHostExecutor();
  phase=new ExecutionPhase(hostExecutors,localCommandFactory,templateDefaults,succeededLogDir,failedLogDir,Suppliers.ofInstance(testBatches),executedTests,failedTests,logger);
  return phase;
}
