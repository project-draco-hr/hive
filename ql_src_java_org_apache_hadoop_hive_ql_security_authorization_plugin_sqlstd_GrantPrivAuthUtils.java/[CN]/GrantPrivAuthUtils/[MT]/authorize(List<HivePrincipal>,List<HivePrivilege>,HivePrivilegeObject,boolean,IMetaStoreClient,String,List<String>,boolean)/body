{
  RequiredPrivileges reqPrivs=getGrantRequiredPrivileges(hivePrivileges);
  checkRequiredPrivileges(reqPrivs,hivePrivObject,metastoreClient,userName,curRoles,isAdmin,HiveOperationType.GRANT_PRIVILEGE);
}
