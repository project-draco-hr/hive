{
  HashCodeBuilder builder=new HashCodeBuilder();
  boolean present_types=true && (isSetTypes());
  builder.append(present_types);
  if (present_types)   builder.append(types);
  return builder.toHashCode();
}
