{
  if (present != null) {
    if (isFileCompressed) {
      index.getNext();
    }
    present.seek(index);
  }
  if (isFileCompressed) {
    index.getNext();
  }
  valueStream.seek(index);
  if (isFileCompressed) {
    index.getNext();
  }
  scaleReader.seek(index);
}
