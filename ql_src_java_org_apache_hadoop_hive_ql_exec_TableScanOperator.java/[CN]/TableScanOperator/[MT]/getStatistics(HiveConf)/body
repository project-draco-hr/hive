{
  Statistics stats=this.getConf().getStatistics();
  if (stats == null) {
    stats=new Statistics();
    stats.addNumberOfBytes(Utilities.getSize(alias,getConf().getTable(),conf,this,getConf().getPruningPredicate()));
    this.getConf().setStatistics(stats);
  }
  return stats;
}
