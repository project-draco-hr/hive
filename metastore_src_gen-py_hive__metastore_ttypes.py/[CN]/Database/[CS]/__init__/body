def __init__(self, d=None):
    self.name = None
    self.description = None
    if isinstance(d, dict):
        if ('name' in d):
            self.name = d['name']
        if ('description' in d):
            self.description = d['description']
