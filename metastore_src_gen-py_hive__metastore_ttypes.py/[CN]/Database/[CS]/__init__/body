def __init__(self, d=None):
    self.name = None
    self.locationUri = None
    if isinstance(d, dict):
        if ('name' in d):
            self.name = d['name']
        if ('locationUri' in d):
            self.locationUri = d['locationUri']
