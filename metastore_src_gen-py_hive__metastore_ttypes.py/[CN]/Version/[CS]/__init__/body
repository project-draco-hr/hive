def __init__(self, d=None):
    self.version = None
    self.comments = None
    if isinstance(d, dict):
        if ('version' in d):
            self.version = d['version']
        if ('comments' in d):
            self.comments = d['comments']
