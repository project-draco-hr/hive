def __init__(self, d=None):
    self.col = None
    self.order = None
    if isinstance(d, dict):
        if ('col' in d):
            self.col = d['col']
        if ('order' in d):
            self.order = d['order']
