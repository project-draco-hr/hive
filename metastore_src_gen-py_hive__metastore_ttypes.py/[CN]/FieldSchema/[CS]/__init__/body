def __init__(self, d=None):
    self.name = None
    self.type = None
    self.comment = None
    if isinstance(d, dict):
        if ('name' in d):
            self.name = d['name']
        if ('type' in d):
            self.type = d['type']
        if ('comment' in d):
            self.comment = d['comment']
