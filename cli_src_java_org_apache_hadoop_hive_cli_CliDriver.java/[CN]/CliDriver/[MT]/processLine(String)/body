{
  int ret=0;
  for (  String oneCmd : line.split(";")) {
    if (oneCmd.equals(""))     continue;
    ret=processCmd(removeComments(oneCmd));
    if (ret != 0) {
      return ret;
    }
  }
  return 0;
}
