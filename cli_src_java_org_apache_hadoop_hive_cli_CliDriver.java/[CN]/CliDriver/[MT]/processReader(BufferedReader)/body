{
  String line;
  StringBuffer qsb=new StringBuffer();
  int ret=0;
  while ((line=r.readLine()) != null) {
    qsb.append(line + "\n");
  }
  ret=processLine(qsb.toString());
  if (ret != 0) {
    return ret;
  }
  return 0;
}
