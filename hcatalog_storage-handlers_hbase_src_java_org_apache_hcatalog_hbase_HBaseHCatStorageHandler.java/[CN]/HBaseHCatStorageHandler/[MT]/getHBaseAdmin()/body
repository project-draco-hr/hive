{
  try {
    if (admin == null) {
      admin=new HBaseAdmin(this.getConf());
    }
    return admin;
  }
 catch (  Exception e) {
    throw new MetaException(StringUtils.stringifyException(e));
  }
}
