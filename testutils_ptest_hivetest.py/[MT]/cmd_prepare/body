def cmd_prepare(patches=[], revision=None):
    get_ant()
    get_arc()
    if args.copylocal:
        copy_local_hive()
    else:
        get_clean_hive()
        patch_hive(patches, revision)
    build_hive()
    propagate_hive()
    prepare_tests()
