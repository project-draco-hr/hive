def propagate_hive():
    print('\n-- Propagating Hive repo to all hosts\n')
    remote_set.run('mkdir -p "{0}"'.format(host_code_path))
    remote_set.run('rsync -qa --delete "{0}/" "{1}"'.format(code_path, host_code_path))
