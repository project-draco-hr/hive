def remove_code():
    print('\n-- Removing Hive code from all hosts\n')
    cmd = (('rm -rf "' + host_base_path) + '"')
    cmd = (('if [[ `echo "{host}" | grep -q -- "-0$"; echo "$?"` -eq "0" ]]; then ' + cmd) + '; fi')
    remote_set.run(cmd)
