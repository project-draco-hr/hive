{
  int listBucketingDepth=0;
  if ((partn.getSkewedColNames() != null) && (partn.getSkewedColNames().size() > 0) && (partn.getSkewedColValues() != null)&& (partn.getSkewedColValues().size() > 0)&& (partn.getSkewedColValueLocationMaps() != null)&& (partn.getSkewedColValueLocationMaps().size() > 0)) {
    listBucketingDepth=partn.getSkewedColNames().size();
  }
  return listBucketingDepth;
}
