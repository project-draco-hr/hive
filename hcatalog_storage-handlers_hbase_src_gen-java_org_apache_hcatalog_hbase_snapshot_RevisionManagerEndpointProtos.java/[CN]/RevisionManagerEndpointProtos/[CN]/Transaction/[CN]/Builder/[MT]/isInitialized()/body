{
  if (!hasTableName()) {
    return false;
  }
  if (!hasTimeStamp()) {
    return false;
  }
  if (!hasKeepAlive()) {
    return false;
  }
  if (!hasRevision()) {
    return false;
  }
  return true;
}
