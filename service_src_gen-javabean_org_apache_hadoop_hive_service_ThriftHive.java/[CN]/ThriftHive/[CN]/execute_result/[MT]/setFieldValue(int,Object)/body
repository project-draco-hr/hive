{
switch (fieldID) {
case EX:
    if (value == null) {
      unsetEx();
    }
 else {
      setEx((HiveServerException)value);
    }
  break;
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
