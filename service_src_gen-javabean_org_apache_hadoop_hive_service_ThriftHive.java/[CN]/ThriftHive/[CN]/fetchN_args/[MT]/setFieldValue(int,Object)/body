{
switch (fieldID) {
case NUMROWS:
    if (value == null) {
      unsetNumRows();
    }
 else {
      setNumRows((Integer)value);
    }
  break;
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}
