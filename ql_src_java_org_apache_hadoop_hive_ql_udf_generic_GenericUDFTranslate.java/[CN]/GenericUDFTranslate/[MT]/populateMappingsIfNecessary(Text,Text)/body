{
  if ((lastFrom == null) || (lastTo == null) || !from.equals(lastFrom)|| !to.equals(lastTo)) {
    populateMappings(from,to);
    if (lastFrom == null) {
      lastFrom=new Text();
    }
    if (lastTo == null) {
      lastTo=new Text();
    }
    lastFrom.set(from);
    lastTo.set(to);
  }
}
