{
  if (this == other)   return true;
  if (!(other instanceof ListTypeInfo)) {
    return false;
  }
  ListTypeInfo o=(ListTypeInfo)other;
  return o.getCategory().equals(getCategory()) && o.getListElementTypeInfo().equals(getListElementTypeInfo());
}
