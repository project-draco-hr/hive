{
  if (fieldIsNull[fieldID])   return null;
  int fieldLen=cachedByteArrayRef[fieldID].getData().length;
  if (fieldLen == nullSequence.getLength() && LazyUtils.compare(cachedByteArrayRef[fieldID].getData(),0,fieldLen,nullSequence.getBytes(),0,nullSequence.getLength()) == 0) {
    return null;
  }
  return fields[fieldID].getObject();
}
