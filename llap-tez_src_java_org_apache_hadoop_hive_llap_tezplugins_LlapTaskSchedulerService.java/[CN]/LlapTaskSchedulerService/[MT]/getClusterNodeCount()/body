{
  readLock.lock();
  try {
    int n=0;
    for (    ServiceInstance inst : activeInstances.getAll()) {
      if (inst.isAlive()) {
        n++;
      }
    }
    return n;
  }
  finally {
    readLock.unlock();
  }
}
