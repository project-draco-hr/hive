{
  if (descs == null) {
    return null;
  }
  final Collection<ExprNodeDescEqualityWrapper> wrapped=new ArrayList<ExprNodeDesc.ExprNodeDescEqualityWrapper>(descs.size());
  for (  ExprNodeDesc desc : descs) {
    wrapped.add(new ExprNodeDescEqualityWrapper(desc));
  }
  return wrapped;
}
