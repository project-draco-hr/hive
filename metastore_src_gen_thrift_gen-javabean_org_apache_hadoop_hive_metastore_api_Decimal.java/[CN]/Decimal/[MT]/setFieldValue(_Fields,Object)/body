{
switch (field) {
case UNSCALED:
    if (value == null) {
      unsetUnscaled();
    }
 else {
      setUnscaled((ByteBuffer)value);
    }
  break;
case SCALE:
if (value == null) {
  unsetScale();
}
 else {
  setScale((Short)value);
}
break;
}
}
