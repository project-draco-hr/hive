{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  LlapNodeId that=(LlapNodeId)o;
  if (port != that.port) {
    return false;
  }
  if (!hostname.equals(that.hostname)) {
    return false;
  }
  return true;
}
