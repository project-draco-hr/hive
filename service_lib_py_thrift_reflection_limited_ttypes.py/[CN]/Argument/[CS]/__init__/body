def __init__(self, d=None):
    self.key = None
    self.name = None
    self.type = None
    if isinstance(d, dict):
        if ('key' in d):
            self.key = d['key']
        if ('name' in d):
            self.name = d['name']
        if ('type' in d):
            self.type = d['type']
