def __init__(self, d=None):
    self.ttype = None
    self.name = None
    if isinstance(d, dict):
        if ('ttype' in d):
            self.ttype = d['ttype']
        if ('name' in d):
            self.name = d['name']
