{
  super.init(m,parameters);
  if (m == Mode.PARTIAL1 || m == Mode.COMPLETE) {
    inputOI=(PrimitiveObjectInspector)parameters[0];
    quantilesOI=parameters[1];
    if (parameters.length > 2) {
      nbinsOI=(PrimitiveObjectInspector)parameters[2];
    }
  }
 else {
    loi=(StandardListObjectInspector)parameters[0];
  }
  if (m == Mode.PARTIAL1 || m == Mode.COMPLETE || quantilesOI.getCategory() == ObjectInspector.Category.LIST) {
    return ObjectInspectorFactory.getStandardListObjectInspector(PrimitiveObjectInspectorFactory.writableDoubleObjectInspector);
  }
 else {
    return PrimitiveObjectInspectorFactory.writableDoubleObjectInspector;
  }
}
