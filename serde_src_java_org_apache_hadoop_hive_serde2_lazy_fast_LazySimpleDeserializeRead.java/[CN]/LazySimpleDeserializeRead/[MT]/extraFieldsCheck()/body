{
  if (offset < end) {
    if (!bufferRangeHasExtraDataWarned) {
      int length=end - start;
      LOG.info("Not all fields were read in the buffer range! Buffer range " + start + " for length "+ length+ " but reading more (NULLs returned)."+ "  Ignoring similar problems.");
      bufferRangeHasExtraDataWarned=true;
    }
  }
}
