{
  this.block=block;
  if (isNew) {
    block.writeShort(O_MAGIC,Magic.FILE_HEADER);
  }
 else   if (!magicOk()) {
    throw new Error("CRITICAL: file header magic not OK " + block.readShort(O_MAGIC));
  }
}
