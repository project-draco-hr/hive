{
  if (init(args) != 0) {
    return 1;
  }
  Var result=run();
  if (result != null) {
    System.out.println(result.toString());
  }
  cleanup();
  printExceptions();
  return getProgramReturnCode();
}
