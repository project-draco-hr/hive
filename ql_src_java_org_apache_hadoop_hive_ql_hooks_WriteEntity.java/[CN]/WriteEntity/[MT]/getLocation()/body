{
  if (typ == Type.TABLE)   return t.getDataLocation();
  if (typ == Type.PARTITION)   return p.getDataLocation();
  if (typ == Type.DFS_DIR || typ == Type.LOCAL_DIR)   return new URI(d);
  return null;
}
