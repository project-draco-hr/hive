{
  if (!abort) {
    reducerHash.flush();
  }
  super.closeOp(abort);
  out=null;
  if (isInfoEnabled) {
    LOG.info(toString() + ": records written - " + numRows);
  }
  recordCounter.set(numRows);
}
