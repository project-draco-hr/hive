{
  if (!abort && reducerHash != null) {
    try {
      reducerHash.flush();
    }
 catch (    IOException e) {
      throw new HiveException(e);
    }
 finally {
      reducerHash=null;
    }
  }
  reducerHash=null;
  super.closeOp(abort);
}
