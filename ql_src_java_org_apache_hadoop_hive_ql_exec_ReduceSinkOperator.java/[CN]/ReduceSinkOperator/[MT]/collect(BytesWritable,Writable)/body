{
  if (null != out) {
    numRows++;
    if (isInfoEnabled) {
      if (numRows == cntr) {
        cntr*=10;
        LOG.info(toString() + ": records written - " + numRows);
      }
    }
    out.collect(keyWritable,valueWritable);
  }
}
