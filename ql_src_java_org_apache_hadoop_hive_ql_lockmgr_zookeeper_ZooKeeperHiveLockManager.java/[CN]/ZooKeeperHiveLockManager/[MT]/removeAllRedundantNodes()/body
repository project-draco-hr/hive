{
  try {
    renewZookeeperInstance(sessionTimeout,quorumServers);
    checkRedundantNode("/" + parent);
    if (zooKeeper != null) {
      zooKeeper.close();
      zooKeeper=null;
    }
  }
 catch (  Exception e) {
  }
}
