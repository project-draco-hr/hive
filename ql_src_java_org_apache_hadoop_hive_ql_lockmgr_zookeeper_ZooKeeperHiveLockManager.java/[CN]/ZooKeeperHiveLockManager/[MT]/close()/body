{
  try {
    if (zooKeeper != null) {
      zooKeeper.close();
      zooKeeper=null;
    }
  }
 catch (  Exception e) {
    LOG.error("Failed to close zooKeeper client: " + e);
    throw new LockException(e);
  }
}
