{
  if (deserializer == null) {
    try {
      deserializer=MetaStoreUtils.getDeserializer(Hive.get().getConf(),getTTable());
    }
 catch (    MetaException e) {
      throw new HiveException(e);
    }
  }
}
