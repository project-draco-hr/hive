{
  if (bucketCols == null) {
    return;
  }
  for (  String col : bucketCols) {
    if (!this.isField(col))     throw new HiveException("Bucket columns " + col + " is not part of the table columns");
  }
  this.getTTable().getSd().setBucketCols(bucketCols);
}
