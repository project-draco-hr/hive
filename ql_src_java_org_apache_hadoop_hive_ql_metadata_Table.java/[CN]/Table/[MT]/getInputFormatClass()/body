{
  if (inputFormatClass == null) {
    try {
      inputFormatClass=(Class<? extends InputFormat>)Class.forName(tTable.getSd().getInputFormat(),true,JavaUtils.getClassLoader());
    }
 catch (    ClassNotFoundException e) {
      throw new RuntimeException(e);
    }
  }
  return inputFormatClass;
}
