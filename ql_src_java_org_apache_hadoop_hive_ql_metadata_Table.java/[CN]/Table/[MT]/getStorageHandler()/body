{
  if (storageHandler != null) {
    return storageHandler;
  }
  try {
    storageHandler=HiveUtils.getStorageHandler(Hive.get().getConf(),getProperty(org.apache.hadoop.hive.metastore.api.Constants.META_TABLE_STORAGE));
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  return storageHandler;
}
