{
  if (DebugUtils.isTraceEnabled()) {
    Llap.LOG.info("close called; current=" + current + ", closed "+ isClosed+ ", done "+ isDone+ ", err "+ pendingError+ ", pending "+ pendingData.size());
  }
  feedback.stop();
  returnAllVectors();
  rethrowErrorIfAny();
}
