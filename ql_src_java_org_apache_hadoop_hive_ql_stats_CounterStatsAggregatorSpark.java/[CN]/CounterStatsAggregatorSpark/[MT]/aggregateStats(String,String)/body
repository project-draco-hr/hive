{
  long value=sparkCounters.getValue(keyPrefix,statType);
  String result=String.valueOf(value);
  LOG.info(String.format("Counter based stats for (%s, %s) are: %s",keyPrefix,statType,result));
  return result;
}
