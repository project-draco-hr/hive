{
  try {
    ZooKeeper zk=getSession();
    String newNode=zk.create(rootNode + NODE_KEYS + "/",s.getBytes(),Ids.OPEN_ACL_UNSAFE,CreateMode.PERSISTENT_SEQUENTIAL);
    LOGGER.info("Added key {}",newNode);
    return getSeq(newNode);
  }
 catch (  KeeperException ex) {
    throw new TokenStoreError(ex);
  }
catch (  InterruptedException ex) {
    throw new TokenStoreError(ex);
  }
}
