{
  if (present != null) {
    if (isFileCompressed) {
      index.getNext();
    }
    present.seek(index);
  }
  if (isFileCompressed) {
    index.getNext();
  }
  stream.seek(index);
  if (isFileCompressed) {
    index.getNext();
  }
  lengths.seek(index);
}
