{
  if (hasLowValue()) {
    if (!getLowValue().isInitialized()) {
      return false;
    }
  }
  if (hasHighValue()) {
    if (!getHighValue().isInitialized()) {
      return false;
    }
  }
  return true;
}
