{
  if (!hasDbName()) {
    return false;
  }
  if (!hasTableName()) {
    return false;
  }
  if (!hasBloomFilter()) {
    return false;
  }
  if (!hasAggregatedAt()) {
    return false;
  }
  if (!getBloomFilter().isInitialized()) {
    return false;
  }
  return true;
}
