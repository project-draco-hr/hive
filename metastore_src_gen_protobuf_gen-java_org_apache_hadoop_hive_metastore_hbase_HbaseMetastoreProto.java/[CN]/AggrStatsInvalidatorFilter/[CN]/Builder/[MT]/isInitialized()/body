{
  if (!hasRunEvery()) {
    return false;
  }
  if (!hasMaxCacheEntryLife()) {
    return false;
  }
  for (int i=0; i < getToInvalidateCount(); i++) {
    if (!getToInvalidate(i).isInitialized()) {
      return false;
    }
  }
  return true;
}
