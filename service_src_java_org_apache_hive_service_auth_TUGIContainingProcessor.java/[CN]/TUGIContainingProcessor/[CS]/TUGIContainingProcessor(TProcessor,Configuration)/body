{
  this.wrapped=wrapped;
  this.isFsCacheDisabled=conf.getBoolean(String.format("fs.%s.impl.disable.cache",FileSystem.getDefaultUri(conf).getScheme()),false);
  this.shim=ShimLoader.getHadoopShims();
}
