{
  SessionHandle sessionHandle=client.openSession(SESSION_USER_NAME,"foobar",Collections.<String,String>emptyMap());
  Assert.assertEquals(1,SessionHookTest.runCount.get());
  client.closeSession(sessionHandle);
}
