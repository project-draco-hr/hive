{
  FileInfo info=null;
  if (context.cacheStripeDetails) {
    info=verifyCachedFileInfo(child.getFileStatus());
  }
  new SplitGenerator(context,fs,child,info,isOriginal,deltas,hasBase).schedule();
}
