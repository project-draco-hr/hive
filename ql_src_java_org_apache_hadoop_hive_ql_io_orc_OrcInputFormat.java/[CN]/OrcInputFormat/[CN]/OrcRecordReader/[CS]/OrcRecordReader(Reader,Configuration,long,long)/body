{
  this.reader=file.rows(offset,length,findIncludedColumns(file.getTypes(),conf));
  this.offset=offset;
  this.length=length;
  if (reader.hasNext()) {
    row=(OrcStruct)reader.next(null);
  }
 else {
    row=null;
  }
}
