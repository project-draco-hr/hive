{
  LinkedHashMap<Operator<? extends OperatorDesc>,OpParseContext> opParseCtxMap=newWork.getOpParseCtxMap();
  QBJoinTree newJoinTree=newWork.getJoinTree();
  MapJoinOperator newMapJoinOp=MapJoinProcessor.convertMapJoin(opParseCtxMap,op,newJoinTree,mapJoinPos,true,false);
  return genLocalWorkForMapJoin(newWork,newMapJoinOp,mapJoinPos);
}
