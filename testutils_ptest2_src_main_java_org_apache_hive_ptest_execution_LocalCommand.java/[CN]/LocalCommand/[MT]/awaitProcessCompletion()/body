{
synchronized (process) {
    if (exitCode == null) {
      exitCode=process.waitFor();
      if (stopwatch.isRunning()) {
        stopwatch.stop();
        logger.info("Finished LocalCommandId={}. ElapsedTime(seconds)={}",commandId,stopwatch.elapsed(TimeUnit.SECONDS));
      }
    }
  }
}
