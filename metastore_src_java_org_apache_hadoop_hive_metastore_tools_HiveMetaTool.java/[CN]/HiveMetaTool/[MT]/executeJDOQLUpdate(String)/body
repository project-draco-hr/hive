{
  long numUpdated=objStore.executeJDOQLUpdate(query);
  if (numUpdated >= 0) {
    System.out.println("HiveMetaTool:Number of records updated: " + numUpdated);
  }
 else {
    System.err.println("HiveMetaTool:Encountered error during executeJDOQL -" + "commit of JDO transaction failed.");
  }
}
