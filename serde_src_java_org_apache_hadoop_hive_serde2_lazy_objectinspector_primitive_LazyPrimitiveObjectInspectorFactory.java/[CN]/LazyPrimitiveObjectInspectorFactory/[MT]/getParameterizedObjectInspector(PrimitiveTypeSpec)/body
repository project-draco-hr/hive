{
  PrimitiveCategory primitiveCategory=typeSpec.getPrimitiveCategory();
  BaseTypeParams typeParams=typeSpec.getTypeParams();
  PrimitiveObjectInspector poi=cachedParameterizedLazyObjectInspectors.getObjectInspector(typeSpec);
  if (poi == null) {
switch (primitiveCategory) {
case VARCHAR:
      PrimitiveTypeEntry typeEntry=PrimitiveObjectInspectorUtils.getTypeEntryFromTypeSpecs(primitiveCategory,typeParams);
    poi=new LazyHiveVarcharObjectInspector(typeEntry);
  poi.setTypeParams(typeParams);
cachedParameterizedLazyObjectInspectors.setObjectInspector(poi);
break;
default :
throw new RuntimeException("Primitve type " + primitiveCategory + " should not take parameters");
}
}
return poi;
}
