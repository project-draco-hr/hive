{
  try {
    Text b=(Text)field;
    bis_.reset(b.getBytes(),b.getLength());
    return this.bt.deserialize(iprot_);
  }
 catch (  Exception e) {
    e.printStackTrace();
    throw new SerDeException(e);
  }
}
