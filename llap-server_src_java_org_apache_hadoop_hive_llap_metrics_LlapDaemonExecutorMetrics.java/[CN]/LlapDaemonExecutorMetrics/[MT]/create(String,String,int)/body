{
  MetricsSystem ms=LlapMetricsSystem.instance();
  JvmMetrics jm=JvmMetrics.create("LlapDaemon",sessionId,ms);
  return ms.register(displayName,"LlapDaemon Executor Metrics",new LlapDaemonExecutorMetrics(displayName,jm,sessionId,numExecutors));
}
