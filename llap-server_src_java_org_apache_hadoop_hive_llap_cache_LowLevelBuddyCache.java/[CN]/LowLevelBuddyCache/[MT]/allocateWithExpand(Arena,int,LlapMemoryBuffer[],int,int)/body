{
  if (arena.data != null)   return ix;
synchronized (arena) {
    if (arena.data == null) {
      arena.init(arenaSize,maxAllocation,arenaSizeLog2,minAllocLog2,maxAllocLog2);
    }
  }
  return allocateWithSplit(arena,freeListIndex,dest,ix,size);
}
