{
  if (o == null) {
    return null;
  }
  HiveChar var;
  if (o instanceof String) {
    var=new HiveChar((String)o,getMaxLength());
  }
 else {
    var=(HiveChar)o;
  }
  return getWritableWithParams(var);
}
