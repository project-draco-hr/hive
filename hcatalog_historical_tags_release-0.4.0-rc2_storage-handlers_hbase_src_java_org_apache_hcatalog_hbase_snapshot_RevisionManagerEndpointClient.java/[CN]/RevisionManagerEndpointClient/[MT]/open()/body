{
  Configuration clonedConf=new Configuration(conf);
  clonedConf.setInt(HConstants.HBASE_CLIENT_RETRIES_NUMBER,1);
  HTable table=new HTable(clonedConf,HConstants.META_TABLE_NAME);
  rmProxy=table.coprocessorProxy(RevisionManagerProtocol.class,Bytes.toBytes("anyRow"));
  rmProxy.open();
}
