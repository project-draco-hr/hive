{
  QueryFragmentInfo queryFragmentInfo=createQueryFragmentInfo(request.getWorkSpec().getVertex(),request.getFragmentNumber());
  return new MockRequest(request,queryFragmentInfo,canFinish,workTime,null);
}
