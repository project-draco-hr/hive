{
  int i=1;
  String name=null;
  for (  SparkTran leaf : leafTrans) {
    name=leaf.getName() + " " + i++;
    leaf.setName(name);
  }
  Set<SparkTran> sparkTrans=transGraph.keySet();
  for (  SparkTran tran : sparkTrans) {
    name=tran.getName() + " " + i++;
    tran.setName(name);
  }
}
