{
  try {
    if (mapJoinTables == null) {
      LOG.debug("mapJoinTables is null");
    }
 else {
      flushToFile();
    }
    super.closeOp(abort);
  }
 catch (  Exception e) {
    LOG.error("Error generating side-table",e);
  }
}
