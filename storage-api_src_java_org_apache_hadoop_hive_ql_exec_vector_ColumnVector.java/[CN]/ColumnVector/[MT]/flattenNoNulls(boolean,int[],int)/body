{
  if (noNulls) {
    noNulls=false;
    if (selectedInUse) {
      for (int j=0; j < size; j++) {
        int i=sel[j];
        isNull[i]=false;
      }
    }
 else {
      Arrays.fill(isNull,0,size,false);
    }
  }
}
