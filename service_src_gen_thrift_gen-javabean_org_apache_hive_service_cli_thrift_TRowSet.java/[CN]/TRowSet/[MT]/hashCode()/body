{
  List<Object> list=new ArrayList<Object>();
  boolean present_startRowOffset=true;
  list.add(present_startRowOffset);
  if (present_startRowOffset)   list.add(startRowOffset);
  boolean present_rows=true && (isSetRows());
  list.add(present_rows);
  if (present_rows)   list.add(rows);
  boolean present_columns=true && (isSetColumns());
  list.add(present_columns);
  if (present_columns)   list.add(columns);
  return list.hashCode();
}
