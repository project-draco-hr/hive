{
switch (field) {
case COL_STATS:
    if (value == null) {
      unsetColStats();
    }
 else {
      setColStats((List<ColumnStatistics>)value);
    }
  break;
case NEED_MERGE:
if (value == null) {
  unsetNeedMerge();
}
 else {
  setNeedMerge((Boolean)value);
}
break;
}
}
