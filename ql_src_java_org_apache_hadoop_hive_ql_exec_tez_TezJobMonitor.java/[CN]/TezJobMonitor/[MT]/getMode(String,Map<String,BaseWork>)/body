{
  String mode="container";
  BaseWork work=workMap.get(name);
  if (work != null) {
    if (work.getLlapMode()) {
      mode="llap";
    }
 else     if (work.getUberMode()) {
      mode="uber";
    }
 else {
      mode="container";
    }
  }
  return mode;
}
