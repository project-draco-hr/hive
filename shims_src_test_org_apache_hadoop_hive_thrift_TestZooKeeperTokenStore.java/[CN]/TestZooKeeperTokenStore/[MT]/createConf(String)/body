{
  Configuration conf=new Configuration();
  conf.set(HadoopThriftAuthBridge20S.Server.DELEGATION_TOKEN_STORE_ZK_CONNECT_STR,"localhost:" + this.zkCluster.getClientPort());
  conf.set(HadoopThriftAuthBridge20S.Server.DELEGATION_TOKEN_STORE_ZK_ZNODE,zkPath);
  return conf;
}
