{
  if (childExpr.size() == 1) {
    return getUnaryFunctionExpression("FuncRound","Double",childExpr,GENERATED_EXPR_PACKAGE);
  }
  long numDigits=getLongScalar(childExpr.get(1));
  VectorExpression e=getUnaryFunctionExpression("RoundWithNumDigits","Double",childExpr,CUSTOM_EXPR_PACKAGE);
  ((ISetLongArg)e).setArg(numDigits);
  return e;
}
