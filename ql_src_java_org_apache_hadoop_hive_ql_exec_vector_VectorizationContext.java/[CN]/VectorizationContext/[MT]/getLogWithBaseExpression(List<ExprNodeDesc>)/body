{
  if (childExpr.size() == 1) {
    return getUnaryFunctionExpression("FuncLn","Double",childExpr,GENERATED_EXPR_PACKAGE);
  }
 else   if (childExpr.size() == 2) {
    String argType=childExpr.get(1).getTypeString();
    double base=getDoubleScalar(childExpr.get(0));
    VectorExpression e=getUnaryFunctionExpression("FuncLogWithBase","Double",childExpr.subList(1,2),CUSTOM_EXPR_PACKAGE);
    ((ISetDoubleArg)e).setArg(base);
    return e;
  }
  throw new HiveException("Udf: Log could not be vectorized");
}
