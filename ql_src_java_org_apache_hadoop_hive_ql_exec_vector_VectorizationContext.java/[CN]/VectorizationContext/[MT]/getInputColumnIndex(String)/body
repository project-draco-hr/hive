{
  if (!columnMap.containsKey(name)) {
    LOG.error(String.format("The column %s is not in the vectorization context column map %s.",name,columnMap.toString()));
  }
  return columnMap.get(name);
}
