{
  if (isStringFamily(javaType)) {
    return "string";
  }
  if (isFloatFamily(javaType)) {
    return "double";
  }
  if (isIntFamily(javaType) || isDatetimeFamily(javaType)) {
    return "bigint";
  }
  throw new HiveException("Unsuported type for vectorization: " + javaType);
}
