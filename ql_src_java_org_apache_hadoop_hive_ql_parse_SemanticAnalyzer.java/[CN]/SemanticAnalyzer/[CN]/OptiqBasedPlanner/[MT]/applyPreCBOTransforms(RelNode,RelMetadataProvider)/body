{
  basePlan=hepPlan(basePlan,mdProvider,HivePushFilterPastJoinRule.JOIN,HivePushFilterPastJoinRule.FILTER_ON_JOIN,new HivePartitionPrunerRule(SemanticAnalyzer.this.conf));
  HiveRelFieldTrimmer fieldTrimmer=new HiveRelFieldTrimmer(null);
  basePlan=fieldTrimmer.trim(basePlan);
  return basePlan;
}
