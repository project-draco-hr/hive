{
  Operator curr=genLimitPlan(dest,qb,input,limit);
  if (isOuterQuery && !sortRequired(dest,qb))   return curr;
  curr=genReduceSinkPlan(dest,qb,curr,1);
  return genLimitPlan(dest,qb,curr,limit);
}
