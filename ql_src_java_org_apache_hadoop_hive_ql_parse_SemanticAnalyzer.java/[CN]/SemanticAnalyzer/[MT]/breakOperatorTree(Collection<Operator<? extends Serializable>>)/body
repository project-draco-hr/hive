{
  if (topOps == null)   return;
  Iterator<Operator<? extends Serializable>> topOpsIter=topOps.iterator();
  while (topOpsIter.hasNext()) {
    Operator<? extends Serializable> topOp=topOpsIter.next();
    breakOperatorTree(topOp);
  }
}
