{
  ASTNode selExprList=qb.getParseInfo().getSelForClause(dest);
  Operator<?> op=genSelectPlan(selExprList,qb,input);
  if (LOG.isDebugEnabled())   LOG.debug("Created Select Plan for clause: " + dest);
  return op;
}
