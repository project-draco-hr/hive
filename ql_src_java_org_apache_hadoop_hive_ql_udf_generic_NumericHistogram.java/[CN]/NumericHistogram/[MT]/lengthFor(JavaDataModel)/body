{
  int length=model.object();
  length+=model.primitive1() * 2;
  int numBins=getNumBins();
  if (numBins > 0) {
    length+=model.arrayList();
    length+=numBins * (model.object() + model.primitive2() * 2);
  }
  length+=model.lengthForRandom();
  return length;
}
