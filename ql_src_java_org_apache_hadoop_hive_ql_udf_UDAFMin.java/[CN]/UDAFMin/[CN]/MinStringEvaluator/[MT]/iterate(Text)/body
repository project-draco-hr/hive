{
  if (o != null) {
    if (mEmpty) {
      mMin=new Text(o);
      mEmpty=false;
    }
 else     if (ShimLoader.getHadoopShims().compareText(mMin,o) > 0) {
      mMin.set(o);
    }
  }
  return true;
}
