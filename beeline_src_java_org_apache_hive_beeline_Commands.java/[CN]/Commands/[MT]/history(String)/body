{
  List hist=beeLine.getConsoleReader().getHistory().getHistoryList();
  int index=1;
  for (Iterator i=hist.iterator(); i.hasNext(); index++) {
    beeLine.output(beeLine.getColorBuffer().pad(index + ".",6).append(i.next().toString()));
  }
  return true;
}
