{
  TaskWrapper taskWrapper;
synchronized (lock) {
    taskWrapper=preemptionQueue.remove();
    if (taskWrapper != null) {
      taskWrapper.setIsInPreemptableQueue(false);
    }
  }
  return taskWrapper;
}
