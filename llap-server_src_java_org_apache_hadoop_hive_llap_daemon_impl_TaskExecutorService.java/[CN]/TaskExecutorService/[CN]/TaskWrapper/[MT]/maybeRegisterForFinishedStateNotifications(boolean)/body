{
  if (!registeredForNotifications) {
    registeredForNotifications=true;
    return taskRunnerCallable.getFragmentInfo().registerForFinishableStateUpdates(this,currentFinishableState);
  }
 else {
    return true;
  }
}
