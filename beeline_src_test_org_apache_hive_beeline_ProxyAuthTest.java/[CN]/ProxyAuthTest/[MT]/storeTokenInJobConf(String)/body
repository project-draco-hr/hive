{
  ShimLoader.getHadoopShims().setTokenStr(ShimLoader.getHadoopShims().getUGIForConf(new Configuration()),tokenStr,HiveAuthFactory.HS2_CLIENT_TOKEN);
  System.out.println("Stored token " + tokenStr);
}
