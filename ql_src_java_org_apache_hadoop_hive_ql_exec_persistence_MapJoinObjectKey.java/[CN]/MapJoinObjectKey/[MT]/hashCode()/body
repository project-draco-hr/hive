{
  int hashCode;
  if (obj == null) {
    hashCode=metadataTag;
  }
 else {
    hashCode=1;
    for (int i=0; i < obj.length; i++) {
      Object o=obj[i];
      hashCode=31 * hashCode + (o == null ? 0 : o.hashCode());
    }
  }
  return hashCode;
}
