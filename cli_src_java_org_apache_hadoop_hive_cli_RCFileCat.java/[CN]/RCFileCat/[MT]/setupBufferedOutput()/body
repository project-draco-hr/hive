{
  FileOutputStream fdout=new FileOutputStream(FileDescriptor.out);
  BufferedOutputStream bos=new BufferedOutputStream(fdout,STDOUT_BUFFER_SIZE);
  PrintStream ps=new PrintStream(bos,false);
  System.setOut(ps);
}
