{
  List<String> args=makeArgs(inputs,output,mapper,reducer,combiner,fileList,cmdenvs,jarArgs);
  JarDelegator d=new JarDelegator(appConf);
  return d.run(user,userArgs,appConf.streamingJar(),null,null,files,args,defines,statusdir,callback,completedUrl,enableLog,jobType);
}
