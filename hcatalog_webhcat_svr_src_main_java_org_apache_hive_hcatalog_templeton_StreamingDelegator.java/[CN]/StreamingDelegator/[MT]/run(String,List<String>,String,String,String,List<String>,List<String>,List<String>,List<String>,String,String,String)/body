{
  List<String> args=makeArgs(inputs,output,mapper,reducer,files,defines,cmdenvs,jarArgs);
  JarDelegator d=new JarDelegator(appConf);
  return d.run(user,appConf.streamingJar(),null,null,null,args,defines,statusdir,callback,completedUrl);
}
