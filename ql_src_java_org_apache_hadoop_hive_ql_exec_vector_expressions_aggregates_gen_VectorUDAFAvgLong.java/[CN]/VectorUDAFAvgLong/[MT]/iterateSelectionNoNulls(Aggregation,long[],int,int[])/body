{
  if (myagg.isNull) {
    myagg.isNull=false;
    myagg.sum=0;
    myagg.count=0;
  }
  for (int i=0; i < batchSize; ++i) {
    long value=vector[selected[i]];
    myagg.sum+=value;
    myagg.count+=1;
  }
}
