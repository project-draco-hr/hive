{
  ColumnInfo existing=get(tabAlias,colAlias);
  if (existing == null) {
    put(tabAlias,colAlias,newCI);
    return true;
  }
  LOG.warn("Found duplicate column alias in RR: " + existing + " for "+ tabAlias+ "."+ colAlias+ " and "+ internalName);
  if (internalName != null) {
    existing=get(tabAlias,internalName);
    if (existing == null) {
      put(tabAlias,internalName,newCI);
      return true;
    }
  }
  LOG.warn("Failed to use internal name after finding a duplicate: " + existing + " for "+ tabAlias+ "."+ internalName);
  return false;
}
