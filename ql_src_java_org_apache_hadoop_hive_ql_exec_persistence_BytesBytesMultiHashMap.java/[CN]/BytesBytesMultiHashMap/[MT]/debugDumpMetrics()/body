{
  if (LOG.isDebugEnabled()) {
    LOG.debug("Map metrics: keys " + this.keysAssigned + ", write conflict "+ metricPutConflict+ ", write max dist "+ largestNumberOfSteps+ ", read neq "+ metricDiffBits+ ", read eq-eq "+ metricSameBitsSameKey+ ", read eq-neq "+ metricSameBitsDiffKey);
  }
}
