{
  if (!hasRows) {
    return true;
  }
  if (!hasList) {
    return (readIndex > 0);
  }
 else {
    if (readIndex <= 1) {
      return false;
    }
 else {
      return (nextTailOffset <= 0);
    }
  }
}
