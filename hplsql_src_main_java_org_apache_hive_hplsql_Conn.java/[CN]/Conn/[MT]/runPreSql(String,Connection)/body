{
  ArrayList<String> sqls=preSql.get(connName);
  if (sqls != null) {
    Statement s=conn.createStatement();
    for (    String sql : sqls) {
      s.execute(sql);
    }
    s.close();
    preSql.remove(connName);
  }
}
