def __init__(self, host, num=None):
    self.host = host
    if (num is None):
        self.hostname = host
    else:
        self.hostname = ((host + '-') + str(num))
    self.pwd = '/'
    self.env = {}
    self.path = []
