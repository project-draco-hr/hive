{
  try {
    if (d != null) {
      runStatementOnDriver("set autocommit true");
      dropTables();
      d.destroy();
      d.close();
      d=null;
    }
  }
  finally {
    TxnDbUtil.cleanDb();
    FileUtils.deleteDirectory(new File(TEST_DATA_DIR));
  }
}
