{
  if (o != null) {
    if (mEmpty) {
      mMax=new Text(o);
      mEmpty=false;
    }
 else     if (ShimLoader.getHadoopShims().compareText(mMax,o) < 0) {
      mMax.set(o);
    }
  }
  return true;
}
