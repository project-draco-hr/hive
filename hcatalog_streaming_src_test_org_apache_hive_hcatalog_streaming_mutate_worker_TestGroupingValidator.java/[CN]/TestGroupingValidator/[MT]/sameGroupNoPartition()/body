{
  assertTrue(validator.isInSequence(Collections.<String>emptyList(),1));
  assertTrue(validator.isInSequence(Collections.<String>emptyList(),1));
  assertTrue(validator.isInSequence(Collections.<String>emptyList(),1));
}
