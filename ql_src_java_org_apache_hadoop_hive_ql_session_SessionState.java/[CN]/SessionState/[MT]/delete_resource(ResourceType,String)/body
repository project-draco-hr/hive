{
  if (resource_map.get(t) == null) {
    return false;
  }
  if (t.hook != null) {
    if (!t.hook.postHook(resource_map.get(t),value))     return false;
  }
  return (resource_map.get(t).remove(value));
}
