{
  if (predicates.size() < 2) {
    return;
  }
  ListIterator<RexNode> iter=predicates.listIterator();
  while (iter.hasNext()) {
    RexNode exp=iter.next();
    if (isAlwaysTrue(exp)) {
      iter.remove();
    }
  }
}
