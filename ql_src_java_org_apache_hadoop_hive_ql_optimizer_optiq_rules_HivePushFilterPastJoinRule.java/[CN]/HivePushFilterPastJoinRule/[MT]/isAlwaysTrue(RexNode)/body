{
  if (predicate instanceof RexCall) {
    RexCall c=(RexCall)predicate;
    if (c.getOperator().getKind() == SqlKind.EQUALS) {
      return isAlwaysTrue(c.getOperands().get(0)) && isAlwaysTrue(c.getOperands().get(1));
    }
  }
  return predicate.isAlwaysTrue();
}
