{
  RexNode andFilters=RexUtil.composeConjunction(rexBuilder,filters,false);
  if (andFilters.isAlwaysTrue()) {
    return rel;
  }
  return new HiveFilterRel(rel.getCluster(),rel.getCluster().traitSetOf(HiveRel.CONVENTION),rel,andFilters);
}
