{
  VectorizedRowBatch vrg=(VectorizedRowBatch)row;
switch (filterMode) {
case 0:
    conditionEvaluator.evaluate(vrg);
  break;
case -1:
vrg.size=0;
break;
case 1:
default :
}
if (vrg.size > 0) {
forward(vrg,null);
}
}
