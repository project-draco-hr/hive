{
  runAs=user;
  List<String> args=makeArgs(jar,mainClass,libjars,files,jarArgs,defines,statusdir,usehcatalog,completedUrl,enablelog,jobType);
  return enqueueController(user,userArgs,callback,args);
}
