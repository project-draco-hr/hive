{
  runAs=user;
  List<String> args=makeArgs(jar,mainClass,libjars,files,jarArgs,defines,statusdir,usesHcatalog,completedUrl,enablelog,enableJobReconnect,jobType);
  return enqueueController(user,userArgs,callback,args);
}
