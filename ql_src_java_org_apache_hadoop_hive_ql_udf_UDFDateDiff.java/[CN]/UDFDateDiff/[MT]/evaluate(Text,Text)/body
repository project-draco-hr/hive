{
  if (dateString1 == null || dateString2 == null) {
    return null;
  }
  try {
    long diffInMilliSeconds=(formatter.parse(dateString1.toString()).getTime() - formatter.parse(dateString2.toString()).getTime());
    result.set((int)(diffInMilliSeconds / (86400 * 1000)));
    return result;
  }
 catch (  ParseException e) {
    return null;
  }
}
