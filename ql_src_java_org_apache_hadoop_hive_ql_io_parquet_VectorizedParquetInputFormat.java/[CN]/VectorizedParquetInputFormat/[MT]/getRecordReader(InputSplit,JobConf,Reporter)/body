{
  try {
    return (RecordReader<NullWritable,VectorizedRowBatch>)new VectorizedParquetRecordReader(realInput,(FileSplit)split,conf,reporter);
  }
 catch (  final InterruptedException e) {
    throw new RuntimeException("Cannot create a VectorizedParquetRecordReader",e);
  }
}
