{
  QueryInfo queryInfo=queryInfoMap.get(queryId);
  if (queryInfo == null)   return null;
  LlapTokenChecker.checkPermissions(clusterId,queryInfo.getTokenAppId(),queryInfo.getTokenUserName(),queryInfo.getQueryIdentifier());
  return queryInfo;
}
