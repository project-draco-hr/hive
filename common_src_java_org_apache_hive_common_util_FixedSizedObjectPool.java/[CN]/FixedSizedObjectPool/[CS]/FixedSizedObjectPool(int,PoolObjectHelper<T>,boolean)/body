{
  if (size > MAX_SIZE) {
    throw new AssertionError("Size must be <= " + MAX_SIZE);
  }
  this.helper=helper;
  @SuppressWarnings("unchecked") T[] poolTmp=(T[])new Object[size];
  pool=poolTmp;
  state=new AtomicLong(OBJECTS.setMarker(0,NO_MARKER));
  casLog=doTraceLog ? new CasLog() : null;
}
