{
  return (Set<String>)cache.retrieve(CACHED_INPUT_KEY,new Callable<Object>(){
    public Object call(){
      return Collections.newSetFromMap(new ConcurrentHashMap<String,Boolean>());
    }
  }
);
}
