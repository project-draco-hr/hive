{
  LOG.error("TezChild execution failed for : " + request.getApplicationIdString() + ", containerId="+ request.getContainerIdString());
  TezChild tezChild=containerRunnerCallable.getTezChild();
  if (tezChild != null) {
    tezChild.shutdown();
  }
}
