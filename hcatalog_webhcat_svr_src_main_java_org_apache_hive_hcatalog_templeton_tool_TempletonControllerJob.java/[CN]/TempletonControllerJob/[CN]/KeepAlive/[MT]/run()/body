{
  try {
    while (sendReport) {
      context.progress();
      System.err.println("KeepAlive Heart beat");
      Thread.sleep(KEEP_ALIVE_MSEC);
    }
  }
 catch (  InterruptedException e) {
  }
}
