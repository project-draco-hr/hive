{
  this.reader=reader;
  this.key=key;
  this.maxKey=maxKey;
  this.bucket=bucket;
  recordReader=reader.rowsOptions(options);
  do {
    next(nextRecord);
  }
 while (nextRecord != null && (minKey != null && key.compareRow(minKey) <= 0));
}
