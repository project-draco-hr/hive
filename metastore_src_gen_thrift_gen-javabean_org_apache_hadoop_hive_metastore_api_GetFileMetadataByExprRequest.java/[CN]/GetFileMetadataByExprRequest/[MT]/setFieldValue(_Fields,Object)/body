{
switch (field) {
case FILE_IDS:
    if (value == null) {
      unsetFileIds();
    }
 else {
      setFileIds((List<Long>)value);
    }
  break;
case EXPR:
if (value == null) {
  unsetExpr();
}
 else {
  setExpr((ByteBuffer)value);
}
break;
}
}
