{
  if (output == null) {
    output=new Output(0);
  }
  key=MapJoinKey.readFromVector(output,key,kw,keyOutputWriters,keyWrapperBatch,!isFirstKey);
  isFirstKey=false;
  this.currentValue=mHash.get(key);
}
