{
  MRUItem entry=mHash.remove(key);
  if (entry != null) {
    MRUList.remove(entry);
  }
 else   if (pHash != null) {
    try {
      pHash.remove(key);
    }
 catch (    Exception e) {
      LOG.warn(e.toString());
      throw new HiveException(e);
    }
  }
}
