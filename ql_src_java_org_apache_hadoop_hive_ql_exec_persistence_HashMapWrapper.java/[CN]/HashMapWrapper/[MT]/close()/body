{
  if (pHash != null) {
    try {
      if (recman != null)       recman.close();
    }
 catch (    Exception e) {
      throw new HiveException(e);
    }
    tmpFile.delete();
    tmpFile=null;
    pHash=null;
    recman=null;
  }
}
