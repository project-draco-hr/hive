{
  if (pHash == null) {
    pHash=getPersistentHash();
  }
  try {
    pHash.put(key,value);
    recman.commit();
  }
 catch (  Exception e) {
    LOG.warn(e.toString());
    throw new HiveException(e);
  }
}
