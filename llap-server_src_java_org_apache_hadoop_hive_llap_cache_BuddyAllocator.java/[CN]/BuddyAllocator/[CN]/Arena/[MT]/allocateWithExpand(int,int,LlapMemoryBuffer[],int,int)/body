{
  if (data == null) {
synchronized (this) {
      if (data == null) {
        init();
        allocatedArenas.incrementAndGet();
      }
    }
  }
  return allocateWithSplit(arenaIx,freeListIx,dest,ix,size);
}
