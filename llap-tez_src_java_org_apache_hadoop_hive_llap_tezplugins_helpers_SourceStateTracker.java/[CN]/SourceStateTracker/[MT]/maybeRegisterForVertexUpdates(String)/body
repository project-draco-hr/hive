{
  if (!notificationRegisteredVertices.contains(sourceName)) {
    notificationRegisteredVertices.add(sourceName);
    taskCommunicatorContext.registerForVertexStateUpdates(sourceName,EnumSet.of(VertexState.RUNNING,VertexState.SUCCEEDED));
  }
}
