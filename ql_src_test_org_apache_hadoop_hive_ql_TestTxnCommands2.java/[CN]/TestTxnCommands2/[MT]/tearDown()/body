{
  try {
    if (d != null) {
      dropTables();
      d.destroy();
      d.close();
      d=null;
      TxnDbUtil.cleanDb();
    }
  }
  finally {
    FileUtils.deleteDirectory(new File(TEST_DATA_DIR));
  }
}
