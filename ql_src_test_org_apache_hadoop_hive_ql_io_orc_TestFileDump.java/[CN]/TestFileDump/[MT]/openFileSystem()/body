{
  conf=new Configuration();
  fs=FileSystem.getLocal(conf);
  fs.setWorkingDirectory(workDir);
  testFilePath=new Path("TestFileDump." + testCaseName.getMethodName() + ".orc");
  fs.delete(testFilePath,false);
}
