{
  int sz=in.readInt();
  if (sz == 0) {
    return new ArrayList<String>();
  }
 else {
    List<String> list=new ArrayList<String>(sz);
    for (int i=0; i < sz; i++) {
      list.add(readStr(in));
    }
    return list;
  }
}
